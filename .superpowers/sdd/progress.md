# SDD Progress — Site Model + Dynamic Posts (sub-project D, increment 4)

Plan: docs/superpowers/plans/2026-07-07-roshambo-site-model-dynamic-posts.md
Base: b647132

(D.1 c8d53d3..5d9433b +hygiene 66980e6; D.2 afa62ac..7593a3c; D.3 3a4df8f..c89221b. roblox-ci green.)

Tasks 1-4 = code (subagent). Tasks 5-6 = Studio MCP capture + wiring + real-perch gate (main session, interactive).

Task 1: complete (commit d30c739, review clean; 306 Lune green, +3). rank S/M/L=1/2/3 + fitsWithin. No issues.
Task 2: complete (commit 50d65cd, review clean; 308 Lune green, +2). PadPlanner omits embed(ground>=datum)/void(nil or >MAXPOST80); PadOps rays from +250 down 350. 4 existing PadPlanner tests still green. No issues.
Task 3: complete (commit b197c6c, review clean; 306 Lune green, RED-before). Size-cap biggest-first onJoin (registry:get/:claim, no claimVacantFor); Action gains footprint; dormantOf renders vacant at site maxSize. No issues.
  Minor(carry to final): onJoin's per-candidate registry:get allocates a fresh table per pad (immaterial at prototype pad counts).
Task 4: complete (commit b197c6c..6df0e51 [amended], review clean; stylua+selene 0/0). buildSupport removed; apply gains footprint, rebuilds structure+dynamic posts transactionally (F4). Stale header comment fixed. No issues.
Task 5: complete (commit 8beaafd) — PadSites baked with T02(L)/T06(M)/T04(S) Deck-anchored mountCFs (MCP-captured), footprint->maxSize.
Task 6: complete (commit 98debd5) — wired real perches (T02 first), threaded Action.footprint, dropped buildSupport call. REAL-PERCH GATE PASSED (user-confirmed): T02 lit L scale 2.0 posts 4/6 (2 embed corners skipped), T06 dormant M 4/6, T04 dormant S 6/6. Archives restored, camera reset.

== ALL 6 D.4 TASKS COMPLETE (b647132..98debd5). Branch CI-equivalent GREEN (306 Lune, stylua+selene 0/0). ==
BACKLOG (D.6 migration curation, user-flagged at gate): terrain maxSize is INSUFFICIENT alone -- a larger footprint can encroach on neighbors' access paths / adjacent teahouses. Real per-site max = min(terrain-max, spacing-max); the baked T02=L/T06=M/T04=S are PROVISIONAL. Far Wall terrain-clearing must mind neighbor spacing too. Add a spacing/collision check to the migration survey.
FINAL whole-branch review (opus, b647132..98debd5): READY TO MERGE — zero Critical/Important.
Verified: footprint threads all 4 apply sites; PadPlanner top[2] == PadOps ray-origin datum (all baked sites yaw-only);
F4 holds with posts added; D.2 race intact; PadBuilder/registry/VacantState shapes correct. Minor/latent findings:
- F1 (D.6 heads-up): PadOps uses ONE fixed ray-origin Y (mountCF.Y+250) for all corners; PadPlanner uses per-corner
  top[2]. They coincide ONLY because all mounts are YAW-ONLY. A PITCHED deck (some of the 14 may be) would skew
  embed/void classification -> D.6 must handle pitched mounts (per-corner datum) OR force yaw-only mounts.
- F2 (Minor): the post-rebuild (PadBuilder.build) in apply's tail is NOT pcall'd and runs after destroying old posts;
  a throw would blank the site + abort the startup vacantActions loop. Not a regression (D.3 buildSupport was same).
  Cheap fix in D.5: wrap it.
- F3 (cleanup, D.5/D.6): PadRegistry.claimVacantFor/fits/claimVacant/findVacant + SizeClasses.nativeSize now DEAD
  (still tested; spec-accepted). Remove in a cleanup pass.
- (out of scope) garden early-return in apply orphans old PadPosts; garden applier deferred, no site uses it.

== SUB-PROJECT D.4 COMPLETE (b647132..98debd5). Final review clean, no fixes needed. ==

== SUB-PROJECT D.5.1 (preference-aware assignment) — plan 892405c ==
Task 1: complete (commit c4a2045, review clean spec+quality). User.padPreferences: string[] default [].
Task 2: complete (commit af7538e, review clean spec+quality). validatePadPreferences + MAX_PREFERENCES/MAX_PREFERENCE_LEN=32.
Task 3: complete (commit d8f7fd5, review clean spec+quality). GET folds padPreferences; PUT /preferences added. Full suite 105/105 green, no fold-through regressions. SERVER SIDE DONE.
Task 4: complete (commit e1b1aa1, review clean spec+quality; size-primary STRUCTURALLY guaranteed). SiteCoordinator._orderedSiteIds + preferences 3rd arg on onJoin; 311 Lune pass, stylua+selene 0/0. nil-path byte-identical.
Task 5: complete (commit 787fbf1, review clean spec+quality). F2 folded: post-rebuild PadBuilder.build pcall'd, [D.5] warn on failure, continues (model.Parent still runs). Studio-only, lint 0/0.
Task 6: complete (commit d1a1e6f, review clean spec+quality). main.server reads padPreferences, passes to onJoin 3rd arg, [D.5] tag. Race guard/onLeave/PlayerRemoving untouched. Studio-only, lint 0/0. CODE COMPLETE (T1-T6); T7 = seed+visual gate remains.
Task 7: GATE ATTEMPT PASSED (awaiting user confirm). Seeded 11060590099 = {M,S}+pref["T06"] (unset stale L via local mongo `roshambo` db; server uses mongodb://mongodb:27017/roshambo NOT the Atlas .env). Play console: "[D.5] 11060590099 claimed T06 @ M". Server state: T06 PointLight ON + Shoji 0.45 (lit); T02 & T04 PointLight off + Shoji 0 (dormant). Preference steered M-tier to T06 over registration-first T02; size-primary holds (M is biggest owned). Play LEFT RUNNING for user to view.

FINAL whole-branch review (opus, 892405c..d1a1e6f): READY TO MERGE — zero Critical/Important. Both suites reproduced green (105 server, 311 Lune). All 7 invariants verified: size-primary STRUCTURAL (order loop outer, order list inner, return-on-first-claim); nil-path exact + []-path also reproduces D.4; GET fold safe (no other consumer); _orderedSiteIds dedup/filter/tail correct; vacant/onLeave untouched; F2 pcall scoped + falls through to model.Parent; validation parity + 401/400. No fixes dispatched.
Minor/informational (non-blocking, backlog):
- _orderedSiteIds nil fast-path returns self._padIds BY REFERENCE (not a copy). Safe today (onJoin read-only); a future mutating caller would corrupt registration order. Optional: shallow-copy or comment.
- new PUT /preferences lets any API_KEY holder set prefs for arbitrary robloxUserId — identical to existing teahouses PUT trust model; acceptable under server-to-server key.

== SUB-PROJECT D.5.1 COMPLETE (c4a2045..d1a1e6f, plan 892405c). Server+Roblox+F2. Final review clean, no fixes. D.5.2 (thumbs UI) and F3 (dead-code cleanup) still deferred. ==

== SUB-PROJECT D.5.2 (perch favoriting) — plan 4422010, base 4422010, branch m4b-zendojo-art-pass ==
Task 1: complete (commit 617f233, review clean spec+quality). PreferenceEditor.togglePreference pure helper (add/remove/dedup/cap), 5 Lune tests, 316 pass, stylua+selene 0/0.
Task 2: complete (commit 44a42a8, review clean spec+quality). NetworkClient:setPreferences PUT /preferences, 2 Lune tests, 318 pass, lint 0/0.
Task 3: complete (commit e48c353, review clean spec+quality). SetPadPreference+PreferenceState remotes; PadSites displayNames (Overlook/Mossy Prow/Creekside). JSON valid, lint 0/0.
Task 4: complete (commit e99d012, review spec ✅ + quality Approved). main.server: playerPrefs cache, join PreferenceState push, SetPadPreference toggle/persist/echo, leave cleanup. Race-guard/onJoin/onLeave preserved verbatim. Studio-only, lint 0/0.
  MINOR (plan-mandated, self-healing, for final-review triage): SetPadPreference writes playerPrefs[uid] back AFTER the yielding net:setPreferences; if the player leaves during the yield, PlayerRemoving nils it but the handler resurrects a stale entry. Both FireClients are IsDescendantOf-guarded (no packet to departed client) and it self-heals next join. Optional fix: guard the post-yield cache write with IsDescendantOf.
Task 5: complete (base 07d778e + fix 30d69b7, review clean spec+quality after fix). PerchPreferenceController: per-perch ProximityPrompt, label flip, toast, SetPadPreference/PreferenceState wiring. FIX 30d69b7: rebind prompt on Structure ChildAdded (was: prompt died on server occupancy-rebuild + guard blocked recreation — real multiplayer gap caught in review). Studio-only, lint 0/0.
  MINOR (harmless, self-healing): stale destroyed-prompt ref in prompts[] during the sub-frame rebuild window; refreshPrompts sets ActionText on a destroyed instance = no-op. Overwritten on rebind.
Task 6: GATE PASSED (user-confirmed). Studio Play against Atlas roshambo-dev, player teleported to T06 (Mossy Prow); ProximityPrompt "♡ Favorite this perch" rendered; user pressed E -> label flip ♥ + toast confirmed; persistence verified: padPreferences=["T06"] in roshambo-dev via full chain. Local server stopped after gate.

FINAL whole-branch review (opus, 4422010..30d69b7): READY TO MERGE — zero Critical/Important. Verified: prefer-only (diff touches only roblox/, D.5.1 route/validator reused); cap 32 consistent; toggle/persist/echo integrity + failure rollback (no toast); PreferenceState payload contract field-for-field server<->client; MaterializedSite_<id> folder/name contract; rebind fix correct (no double-create, no leak); D.4/D.2 race guard + leave cleanup preserved; per-client prompts safe on populated server.
Minor (non-blocking, self-healing, backlog):
- rapid same-perch double-toggle can interleave 2 in-flight PUTs; last-to-land wins, cache/DB briefly diverge, self-heals next join. Optional: prompt HoldDuration or per-uid in-flight guard.
- leave-mid-persist + PUT failure resurrects one stale playerPrefs entry (Task-4 known); bounded, self-heals next join.

== SUB-PROJECT D.5.2 COMPLETE (617f233..30d69b7, plan 4422010). Perch favoriting: pure toggle + NetworkClient PUT + remotes + server handler + client prompts/toast + rebind fix. Gate passed (user-confirmed ♡→♥ + toast, padPreferences=["T06"] in roshambo-dev). Final review clean. D.5 (preference) DONE. Remaining D: D.6 migration sweep + F3 dead-code cleanup.

== SUB-PROJECT D.6.1 (site-fit machinery) — plan b5561a3, base b5561a3, branch m4b-zendojo-art-pass ==
Task 1: complete (module 6505baa + renorm-coverage test 7c36d94, review clean spec+quality; geometry hand-traced). PadSiteFit: overlaps(SAT)/footprintOBB/spacingMax/isLevel/normalizeYaw/resolveMaxSize, no trig, 337 Lune pass, lint 0/0.
Task 2: complete (commit b8db9bd, review clean spec+quality). F3: removed nativeSize+require, PadRegistry.fits/findVacant/claimVacant/claimVacantFor + their tests; release test asserts via get().occupant; SizeClasses decoupled from PadRegistry. 328 Lune pass, lint 0/0.

FINAL whole-branch review (opus, b5561a3..b8db9bd): READY TO MERGE — zero Critical/Important. Verified: NO trig in PadSiteFit (only abs/sqrt — no CI-drift-class risk); CFrame convention matches PadPlanner.xf; normalizeYaw basis orthonormal + identity-on-level (hand-traced); overlaps is true OBB-SAT (edge-contact=overlap); F3 zero remaining refs, kept API intact + still used by SiteCoordinator/main.server, SizeClasses decoupled from PadRegistry; 328 Lune pass, lint 0/0.
Minor (non-blocking, for D.6.2 survey to heed):
- normalizeYaw divides by len with no zero-guard -> NaN for a 90-deg-pitch mount (fx=fz=0). Contract is "near-level only"; the D.6.2 survey MUST isLevel-gate before calling normalizeYaw (or add a guard when it gets a caller).
- PadRegistry still exports now-unused Footprint type (harmless; sweep later).
- PadSiteFit.spec "far" diamond comment overstates ("AABB grazes"); assertion correct.

== SUB-PROJECT D.6.1 COMPLETE (6505baa..b8db9bd, plan b5561a3). Site-fit machinery: PadSiteFit (OBB-SAT spacing, isLevel/normalizeYaw, resolveMaxSize) + F3 cleanup. Final review clean, no fixes. Remaining D.6: D.6.2 Studio survey (produce keep-outs, re-bake PadSites maxSize=min(terrain,spacing), isLevel-gate mounts) + legacy retirement + Far Wall access.

== D.6.2 SURVEY CONVENTIONS (settled with user 2026-07-08 — bake into the D.6.2 spec) ==
- SEQUENCING: build Far Wall paths/access FIRST, then survey+migrate. The survey CONSUMES path geometry as keep-outs. Self-consistent: paths route to fixed perch entrances (size-independent); survey caps each teahouse maxSize to fit the built paths; legacy teahouse size is irrelevant. Reason paths-first (not reserve-then-build): the Far Wall access is a switchback network weaving PAST multiple perches, so real routing constrains footprints in ways per-perch reserved rectangles can't capture.
- KEEP-OUT TAG: CollectionService tag `AccessKeepOut`, applied by the path-build tools as they create parts (hierarchy-independent -> survives migration's reparent/retire of legacy teahouses). Optional `Workspace.CanyonAccess` folder for tidiness, but the survey relies on the TAG, not the folder. Query via CollectionService:GetTagged("AccessKeepOut").
- SCOPE = FULL BUILT ENVELOPE (user chose): tag walkable surfaces (path treads, deck boards, bridge decks, stairs) AND path-edge structures (railings, path-side lanterns, access-bordering retaining walls). DO NOT tag: the perch's own deck/pad (= mountCF datum, known from PadSites), its back/side retaining walls, terrain, or the legacy CanyonTeahouses (neighbor spacing is computed from PadSites, not tagged geometry).
- EXTRACTION: for each tagged BasePart, survey builds its world-XZ OBB via PadSiteFit.footprintOBB({minX=-SizeX/2,maxX=SizeX/2,minZ=-SizeZ/2,maxZ=SizeZ/2} at the part's 12-num CFrame), inflated ~1.5-2 studs standoff (matches the walls-register-+1.5-2 rule). Test each perch's candidate footprint vs ALL tagged OBBs (no spatial filter needed; distant ones clear trivially).
- BUILD FOR INTENDED SIZE: route/clear for the intended post-clearing max per perch, not the legacy footprint. Terrain-clearing raises terrain-max (raycast), it is NOT a keep-out.
- GATE: the survey MUST PadSiteFit.isLevel-gate each mount before normalizeYaw (D.6.1 backlog: normalizeYaw has no zero-guard for a 90-deg-pitch mount).

== SUB-PROJECT: WORKSPACE ORGANIZATION (Rojo/place-only convention) — plan 93a7bf0, base 93a7bf0, branch m4b-zendojo-art-pass ==
Spec 592a5ca, plan 93a7bf0. Tasks: 1 StagePersistence helper+wire-in; 2 CI guard (no committed .rbxl); 3 WorkspaceConvention checker+Studio verifier; 4 Studio re-parent (INTERACTIVE); 5 verify (INTERACTIVE); 6 docs+memory.
Task 1: complete (commit d99b7ea, review clean spec+quality, 332 Lune pass, lint 0/0). StagePersistence.persistTargets(roots)->{Model} pure helper + main.server.luau wire-in (RoshamboStage WaitForChild + CanyonWorld/Arena FindFirstChild nil-guarded, deploys safely pre-reorg). Reviewer note: brief's "tabs" claim was stale, repo uses 4-space (impl followed real stylua.toml).
  MINOR (backlog, cosmetic): main.server.luau two adjacent persistence comment blocks overlap; wire-in path itself not unit-tested (Studio-dep, acceptable).
Task 2: complete (commit 9f4a178, review clean spec+quality, no issues). CI step "No committed place files" in roblox-ci.yml test job, working-directory override to repo root, git ls-files *.rbxl(x) -> exit 1 on match. Dry-run verified catch+clean. Rojo-build step untouched.
Task 3: complete (commit e7eb5f8, review clean spec+quality, 337 Lune pass, lint 0/0). WorkspaceConvention.check(snapshot)->(ok,failures) pure checker (5 failure modes: undeclared/missing stage child, missing CanyonWorld, missing Arena, empty Arena) + DECLARED_STAGE_CHILDREN (8, cross-checked vs project.json exact) + tools/studio/verifyWorkspaceConvention.luau adapter (requires shared module DRY, snapshots live Workspace). 
  MINOR (backlog, brief-inherited): "missing declared child" + "empty Arena" tests assert only the boolean not failure count (mutant returning (false,{"x"}) would pass); no multi-violation combo test.
Task 6: complete (commit 19a3545, review clean, no issues, all claims verified vs repo). CLAUDE.md "### Workspace organization (Rojo vs place-only)" subsection + README_DEPLOY.md "### 0c. Before publishing the Roblox place". MEMORY update (Step 4) DEFERRED to post-reorg (Task 5 verify) so it reflects reality not aspiration.
CODE/DOC TASKS 1,2,3,6 ALL COMPLETE + reviewed clean. Tasks 4 (Studio re-parent) + 5 (verify) are INTERACTIVE — pending user session with Studio.

FINAL whole-branch review (opus, 93a7bf0..19a3545, code/doc tasks only — reorg is place-only, no diff): READY TO MERGE, zero Critical/Important. Verified: shared defined before wire-in (no ordering hang); 8 DECLARED names == project.json exact+order; both requires resolve (src/shared->RoshamboShared); CI lint covers tools/; RoshamboStage persist set byte-for-byte preserved (persistRoots starts {stage}, same GetChildren/IsA order, arena only appended); pre-reorg no-hang confirmed (WaitForChild only for always-present RoshamboStage, FindFirstChild+nil-guard for CanyonWorld/Arena); verifier snapshot<->checker contract matches; CI guard fails-on-match/pass-otherwise, rojo-build artifact untracked=no false positive; docs match code. 3 known Minors all ACCEPTABLE AS BACKLOG (comment overlap; wire-in not unit-tested [Studio-dep]; 2 spec asserts boolean-only but count pinned by sibling tests).
Task 4: complete (INTERACTIVE, place-only — no git diff; user saved place). Built CanyonWorld{Arena,Paths,Structures,Legacy}+Sandbox skeleton; moved 24->Arena, 12->Paths, 4->Structures (EngawaSafetyRail absent - gone since earlier session), CanyonTeahouses->Legacy, 5(4 POC+DevChannelSpawn)+3 drafts->Sandbox; deleted orphaned root Decal; VibrantNature->Sandbox then DELETED (see below). RoshamboStage == exactly 8 declared. Workspace root clean: RoshamboStage/CanyonWorld/Sandbox/Terrain/Camera/ThumbnailCamera.
Task 5: complete (INTERACTIVE). Convention verify PASS (inline mirror); negative check trips FAIL then restores; StagePersistence+WorkspaceConvention confirmed synced into place (Rojo connected). Play-test deferred (low-risk, place-only positions preserved; user saved). Memory roblox-rojo-vs-place-state REWRITTEN to declarative framing + new folders + guards; MEMORY.md index updated.
SECURITY (out-of-band, user-requested during Task 4): VibrantNature toolbox pack (2767 inst) carried a require-backdoor virus — Script "CoreTextureSystem" (@ovenv3), payload require(<attr Version=119562760813431>) hidden 12 levels deep in a PalmTree mesh, IsStudio-guarded (dormant in Studio, fires only live), decoy child ModuleScript "TextureConfiguration", ~200 lines light-mgmt camouflage. Place never published => never ran. Whole pack DELETED; full-place rescan 0 suspicious remaining (only our RibbonFlowAnim/HideDevMarkers left, clean). New memory roblox-toolbox-backdoor-scan created.

== SUB-PROJECT WORKSPACE ORGANIZATION COMPLETE (code d99b7ea..19a3545, plan 93a7bf0; reorg place-only saved). Tasks 1-6 all done+reviewed; final whole-branch review READY TO MERGE (0 Critical/Important, 3 backlog Minors). Plus VibrantNature backdoor removed. Branch m4b now ~10 commits ahead of origin (UNPUSHED). Backlog Minors: main.server.luau comment-block overlap; wire-in not unit-tested (Studio-dep); WorkspaceConvention.spec 2 boolean-only asserts.

== D.6.2 FAR-WALL PAD SURVEY (in progress, interactive Studio; size classes retuned M=1.3/L=1.6 commit 0fbae1c) ==
Method: draggable PadMarker (green L slab + front arrow + magenta DatumPin) at each far-wall legacy teahouse datum (Perim-underside, model yaw); user positions for cut-vs-void balance; cut-only Air shave (snapshot to ServerStorage.FarWallClearing_Backups.<TH> first) then re-probe; bake mount when 0-cut clean. Far-wall perches: Teahouse_08/09/10/11/12/13. To be batch-written into src/server/PadSites.luau (id T08..T13) at end; displayNames TBD by user; vacantForm="dormant-structure".
T11 BAKED (cleared to L, 0 cut): mountCF = { -192.0705, 176.4412, -52.6185, -0.7158, 0, -0.6983, 0, 1, 0, 0.6983, 0, -0.7158 }, hand=right, maxSize=L. Terrain shaved (backup ServerStorage.FarWallClearing_Backups.Teahouse_11). Place needs SAVE.
T10: marker up, hand=right, start mount (-187.3, 209.0, -95.0) — surveying next.
PadRef stubs: on each bake, drop Workspace.Sandbox.PadRefs.PadRef_<id> (collidable teal L-slab + cyan outline + front arrow, attrs maxSize/hand) at the baked mount = the ACCESS TARGET for phase 2 (legacy models stay out-of-position and get retired). PadRef_T11 placed. Place needs SAVE.
T10 BAKED (as-is, NO shave — user accepted): mountCF = { -185.8742, 208.9550, -95.5316, -0.9331, 0, -0.3595, 0, 1, 0, 0.3595, 0, -0.9331 }, hand=right, maxSize=L. As-built: 1 back corner (~34 studs) lodges into the cliff (kake-zukuri, intentional, unshaved); ~5 samples cantilever over ~25-stud void (long stilts). PadRef_T10 placed.
T09: marker up, hand=right, start mount (-276.9, 207.8, -59.7) — surveying next.
T09 BAKED (shaved, snapshot ServerStorage.FarWallClearing_Backups.Teahouse_09): mountCF = { -276.9496, 202.7953, -55.4460, -1.0000, 0, 0.0030, 0, 1, 0, -0.0030, 0, -1.0000 }, hand=right, maxSize=L. Post-carve ~voxel residue (9 samples max 4.2, user to smooth); ~8 samples cantilever ~23-stud void. PadRef_T09 placed.
T08: marker up, hand=right, start mount (-340.3, 220.6, -89.4) — surveying next. DONE so far: T11,T10,T09 (L). Remaining: T08,T12,T13.
T08 BAKED (clean, NO shave, 0 cut/0 void): mountCF = { -338.7201, 220.6116, -87.8492, -1.0000, 0, 0, 0, 1, 0, 0, 0, -1.0000 }, hand=right, maxSize=L. PadRef_T08 placed.
T12: marker up, hand=right, start mount (-122.7, 156.9, -57.2) — surveying next. DONE: T11,T10,T09,T08 (all L). Remaining: T12,T13.
T12 BAKED (clean, NO shave, ~0 cut/0 void, near-level terrace): mountCF = { -122.2799, 156.8501, -55.7121, -0.9602, 0, -0.2793, 0, 1, 0, 0.2793, 0, -0.9602 }, hand=right, maxSize=L. PadRef_T12 placed.
T13: marker up (LAST), hand=right, start mount (3.0, 198.3, -144.2) — surveying. DONE: T11,T10,T09,T08,T12. Remaining: T13.
T13 BAKED (clean, rotated along contour, ~0 cut/0 void): mountCF = { 5.5001, 198.2727, -144.4534, -0.9939, 0, -0.1104, 0, 1, 0, 0.1104, 0, -0.9939 }, hand=right, maxSize=L. PadRef_T13 placed.

=== FAR-WALL PAD SURVEY COMPLETE (2026-07-08): T08,T09,T10,T11,T12,T13 all maxSize=L, hand=right, PadRefs in Sandbox/PadRefs. ===
NEXT: (1) batch-write these 6 into src/server/PadSites.luau (ids T08-T13, displayNames TBD); (2) SAVE THE PLACE (T09/T11 shaves + 6 PadRefs are place-only; terrain backups in ServerStorage.FarWallClearing_Backups.{Teahouse_09,Teahouse_11}); (3) near-wall T02/T04/T06 still carry OLD-survey maxSize at old L=2.0 sizes — due for re-survey at new 1.6/1.3; (4) then access/paths phase to the PadRefs.
FAR-WALL PADS BAKED INTO PadSites.luau (commit 725cb8c): T08-T13 maxSize=L hand=right, placeholder displayNames "Far Perch NN". 337 Lune pass, lint clean. Place saved by user (shaves+PadRefs persisted). Branch m4b now ~14 unpushed. NEXT PHASE: far-wall access paths to the PadRefs (+ near-wall T02/04/06 re-survey; legacy retirement).

== NEAR-WALL PAD SURVEY (in progress, fixed-POSITION re-probe; ORIENTATION corrected per-perch — veranda must face canyon, else footprint asymmetry digs into hill). Perches T01-07 + T14. T02/04/06 are re-surveys (replace old entries). Left-hand (mirror footprint X): T03,T05,T06. ===
T01 BAKED (rotated veranda to face canyon -> cut 33->1, clean L; pos NOT snapped to legacy, drifted ~3 studs — has an access tunnel, minor tweak may follow): mountCF = { 39.6297, 200.6605, 129.2485, 0.9404, 0, 0.3401, 0, 1, 0, -0.3401, 0, 0.9404 }, hand=right, maxSize=L. PadRef_T01 placed.
T02: marker up (re-survey), hand=right, legacy datum (-142.4, 172.2, 70.0) — surveying next.
LESSON: orient veranda to the view FIRST, then read terrain (footprint is asymmetric, deep side = veranda).
T02 BAKED (re-survey, clean L, facing already correct): mountCF = { -140.6872, 172.1823, 68.6440, 0.9754, 0, 0.2204, 0, 1, 0, -0.2204, 0, 0.9754 }, hand=right, maxSize=L. PadRef_T02 placed. (Replaces old PadSites T02 entry at batch-write.)
T03: marker up (left-hand, footprint mirrored, hand-detect confirmed engawa wide -X), legacy datum (-194.6, 187.9, 61.9) — surveying next.
T03 EXCEPTION (user 2026-07-08): switched to FREE-REPOSITION (not fixed) — its access bridge lands at a specific point hard to match across sizes; user chose to reposition the pad aggressively for a good L fit and REWORK the bridge (+ any stair/landing) later. ACCESS-REWORK BACKLOG: T03 bridge touchdown + stair/landing must be re-landed to the repositioned mount in the access phase.
T03 BAKED (FREE-REPOSITIONED off the void + lowered -> cut 0, void 0, clean grounded L; left-hand): mountCF = { -191.5580, 181.1997, 59.9253, 0.9195, 0, -0.3931, 0, 1, 0, 0.3931, 0, 0.9195 }, hand=left, maxSize=L. PadRef_T03 placed. ACCESS REWORK owed (bridge + stair/landing) to new mount.
T04: marker up (right-hand), legacy datum (-190.7, 216.9, 94.0). Had big 38-sample cantilever at legacy. Access = CURVED BURIED TUNNEL (reposition => tunnel rework). Surveying next.
T04 BAKED (repositioned ~4 off legacy + user ACCEPTS the deep void = deliberate cantilever prow; right-hand): mountCF = { -191.8544, 216.9268, 98.1871, 0.9634, 0, -0.2680, 0, 1, 0, 0.2680, 0, 0.9634 }, hand=right, maxSize=L. void ~29 samples/26-drop (intentional). PadRef_T04 placed. ACCESS REWORK owed: curved buried tunnel re-bore to new mount.
T05: marker up (left-hand, mirrored), legacy datum (-322.0, 216.7, 85.3) — surveying next.
T05 BAKED (repositioned+lowered off legacy, clean L, left-hand; access was FUTURE tunnel = no rework owed): mountCF = { -314.8236, 219.2089, 92.2358, 0.9907, 0, -0.1363, 0, 1, 0, 0.1363, 0, 0.9907 }, hand=left, maxSize=L. PadRef_T05 placed.
T06: marker up (left-hand, mirrored), legacy datum (-381.4, 222.1, 64.8) — surveying next. DONE near-wall: T01-05. Remaining: T06, T07, T14.
T06 BAKED (re-survey, L, left-hand; L posts all clear NW80FallsStair, only M-downsize FL post catches it -> handled by planner-omit rule below): mountCF = { -380.0815, 222.0902, 67.5655, 0.7290, 0, -0.6845, 0, 1, 0, 0.6845, 0, 0.7290 }, hand=left, maxSize=L. PadRef_T06 placed. (Replaces old PadSites T06 entry.)

** DESIGN DECISION (user-adopted 2026-07-08): PATH-OMIT RULE for PadPlanner. **
PadPlanner.planSupport currently omits a post when terrain is embedded (ground>=datum) or void (nil / >MAXPOST). ADD: omit a post that would land on an AccessKeepOut-tagged path (deck cantilevers over it), exactly like void. Rationale: a post dropping onto a walkable path is as bad as a void post; systematically dissolves path-post conflicts (found at T06: NW80FallsStair runs under the front edge, mid-size FL post lands on it, no all-sizes-clear mount exists). IMPLEMENTATION (D.6.2 planner code task, TDD, keep PadPlanner pure): inject a keep-out predicate e.g. hitsKeepOut(worldX, worldZ, topY, groundY)->boolean alongside groundAt; omit the post if true; add it to `omitted`. Runtime wires it to a raycast vs CollectionService:GetTagged("AccessKeepOut"); tests use a fake predicate. NOTE: survey's post-vs-path check stays useful (ensure a pad isn't LEFT with too few posts once path-posts are omitted).
T07: marker up (right-hand), legacy datum (-322.1, 192.3, 44.3) — surveying next. DONE near-wall: T01-06. Remaining: T07, T14.
T07 BAKED (radically repositioned ~30 off legacy, flawless: 0 cut/0 void, all 6 posts clear ALL sizes L/M/S vs paths, right-hand): mountCF = { -291.6609, 189.1661, 39.5640, 0.9652, 0, 0.2615, 0, 1, 0, -0.2615, 0, 0.9652 }, hand=right, maxSize=L. PadRef_T07 placed. (If it had built access, rework owed to new mount.)
T14: marker up (LAST near-wall = former TeahousePrototype), hand=right, legacy datum (15.4, 175.3, 97.7) — surveying next.
T14 BAKED (relocated ~97 studs off legacy, clean L, all sizes clear, right-hand): mountCF = { 110.5484, 188.3997, 104.4366, 0.9232, 0, 0.3843, 0, 1, 0, -0.3843, 0, 0.9232 }, hand=right, maxSize=L. PadRef_T14 placed. ACCESS: bridge to NW_11 needs full rework (moved far).

=== NEAR-WALL PAD SURVEY COMPLETE (2026-07-08): T01-T07 + T14, all maxSize=L. Hands: left={T03,T05,T06}, right={T01,T02,T04,T07,T14}. All 14 PadRefs (T01-T14) in Sandbox/PadRefs. ===
NEXT: batch-write 8 near-wall into PadSites.luau (T02/04/06 replace old entries keeping displayNames Overlook/Creekside/MossyProw; T01/03/05/07/14 new placeholder names). Then SAVE PLACE. ACCESS-REWORK BACKLOG (repositioned perches): T03 bridge+stair/landing, T04 curved tunnel, T14 bridge; also T01 minor (drifted ~3, tunnel). T05/T07 access was future/none.
PATH-OMIT PLANNER FIX DONE (commit 5d6c2ba, 341 Lune pass, lint clean): PadPlanner.planSupport gained optional blocked(x,z,topY,groundY) predicate (pure, 5 new tests w/ fake); PadBuilder threads ops.blockedByPath; PadOps implements raycast vs CollectionService:GetTagged("AccessKeepOut") in the post column. DORMANT until paths get the AccessKeepOut tag in the access phase (then T06-style path-post conflicts auto-omit + cantilever). Branch m4b ~18 unpushed.
T14 ACCESS TUNNEL BORED (user-confirmed walk 2026-07-08): branches S off the existing T01 tunnel (T-junction at marker _0), loops south (apex _2) to stay buried, exits at the T14 pad SW corner (_3). Catmull-Rom floor-follow, 11w x 12t, mouths +6, 115 carve steps, verified 38/38 center points open. Snapshot ServerStorage.T14Tunnel_Backup. Draft markers at Sandbox/T14TunnelDraft (can clear). PLACE-ONLY -> SAVE. ACCESS-REWORK BACKLOG UPDATE: T14 access now DONE (this tunnel replaces the old NW_11 bridge, which user is deleting). Still owed: T03 (bridge+stair/landing), T04 (curved tunnel), T01 (minor, drifted ~3).
ACCESS BACKLOG UPDATE (user 2026-07-08): T04 existing curved tunnel STILL WORKS after the ~4-stud reposition -> NO rework (removed from backlog). Remaining owed: T03 (bridge+stair/landing, big reposition), T01 (minor mouth check). T14 done (new tunnel).
ACCESS BACKLOG UPDATE (user 2026-07-08): T01 tunnel connects PERFECTLY to the repositioned pad -> NO tweak needed. REMAINING ACCESS-REWORK BACKLOG = T03 ONLY (bridge + stair/landing, big reposition). T04 fine, T14 new tunnel done, T01 fine.

== SESSION WRAP 2026-07-08 ==
DONE this session: size-class retune M=1.3/L=1.6 (0fbae1c); FULL pad survey — all 14 perches baked at L into PadSites.luau (far-wall T08-13 725cb8c; near-wall T01-07,T14 13e066f); 14 PadRef access-target stubs in Sandbox/PadRefs; path-omit planner fix (PadPlanner blocked predicate + PadBuilder/PadOps wiring, 5d6c2ba, dormant until paths tagged AccessKeepOut); T14 access tunnel bored (S off T01 tunnel, place-only). Also: NW30->NW62 straight tunnel BORED 7x7 (48.8deg, place-only, bare terrain floor, backup ServerStorage.NW30_62Tunnel_Backup) — STAIRS ABANDONED (plank + haphazard-timber both rejected; use the ishidan recipe when revisited). Workspace-organization sub-project also completed+reviewed earlier (READY TO MERGE).
STATE: branch m4b ~19 commits ahead of origin (UNPUSHED). Lune 341 pass, lint clean. Place saved by user (confirm on restart). Access-rework backlog = T03 only (bridge+stair/landing). Draft markers: Sandbox/PathDraft (FarWall_* + NearWall_*), Sandbox/T14TunnelDraft (can clear).
NEXT SESSION (user's pick): FAR-WALL PATHS + ACCESS. Build the FarWall trunk (FarWall_50->82 markers exist) + access to the far-wall PadRefs (T08-T13); tag all path/access parts AccessKeepOut as built (activates the planner path-omit rule). Consult zendojo-build-recipes + zendojo-ishidan-step-recipe first. Then legacy CanyonTeahouses retirement.

---
# SDD Progress — Pad/Deck Separation (Piece A), Phase 1

Plan: docs/superpowers/plans/2026-07-11-pad-deck-separation.md
Base: fb87501
Phase 1 (Tasks 1-3): pure Lune-tested foundation (deckFootprint, BuildingPlacer transform+fit). Run: `lune run tests/run` from roblox/.
Task 1: complete (commit 6284724, review clean; 345 Lune green, +3). deckFootprint symmetric 26x20 sqrt-area; legacy footprint untouched.
Task 2: complete (commit d60e01f, review clean; 348 Lune green, +3). BuildingPlacer.facingYaw N/E/S/W=0/90/180/270 + placeCF row-major {dx,0,dz,c,0,s,0,1,0,-s,0,c}. Pure.
Task 3: complete (commit 5df53fb, review clean; 353 Lune green, +5). BuildingPlacer.fits AABB containment, swaps X/Z for E/W, reuses facingYaw. Pure.
== PHASE 1 COMPLETE (fb87501..5df53fb). 3 pure modules, all reviews clean, 353 Lune green (+11 from 342). Phases 2-3 (procedural deck geometry + prefab re-capture + wiring) remain — visual-gated, need Studio. ==

--- Phase 2 (Tasks 4-6): procedural deck geometry, VISUAL-GATED (need Studio). ---
Task 4: complete (commit 482c39a, review clean; 355 Lune green, +2). PadFrame.nodes (6 nodes inset PH=0.6, IDs FL/FR/BL/BR/MF/MB) + beams (2 long+3 short). Pure, no requires.
  Minor(carry to final): test asserts the formula (same arithmetic as impl) rather than independent values, and doesn't cover BL/MB coords — inherited from brief. No explicit return-type annotations (relies on inference, --!strict passes).
Tasks 5-6 REMAIN: visual-gated Studio work (procedural deck slab+beams+railings; strip deck from teahouse-1story prefab + re-capture). Need the user at Studio.
Task 5: complete (commit cb8fff8, VISUAL GATE PASSED user-confirmed; 358 Lune green +3, stylua+selene clean). PadBuilder.buildDeck (slab+5 beams+4 engawa rails+6 posts); PadOps buildDeckSlab/buildBeam/buildRailing; rails matched to tuned teahouse-1story koran (wood 74,55,38 / charcoal cap 45,48,56 / ~3.76 baluster c-c / newels flush to cap top, cap+mid butt between newels); buildPost applies mount yaw (posts square to deck). PadBuildDeck.spec locks call graph. PadFrame exports Node type.
  User iterations during gate: (1) rails didn't match legacy engawa -> matched koran color/spacing; (2) newels too high + cap pierced them -> newels flush, cap butts between; (3) posts world-axis -> apply mountCF.Rotation.
RE-BAKE (commit dc8f30d, separate): all 14 PadSites mountCF X/Z -> deck center (from PadRef_Txx.Pad); Y/rot/hand/maxSize/names unchanged; uniform ~8.5 shift (T11 ~7.0); all 14 seat 0.000 offset, posts mostly 6/6 (T08 5/6 void cantilever). Fixes the whole class of stray-post/left-hand offset bugs at the data level.
Tasks 4-5 DONE + re-bake. Task 6 REMAINS: strip deck/engawa from teahouse-1story prefab (captureTeahouseBase.luau + Studio re-capture, visual gate). Then Phase 3 (wire placement + two-input materialization).
NOTE: preview geometry at workspace.PadDeckPreview (14 L decks) is throwaway place-only — clear before saving the place.
Task 6: complete (commit 7455efb, VISUAL GATE PASSED user-confirmed). captureTeahouseBase.luau strips all deck/veranda (Deck/EngawaF/S/Perim*/Joist*/Girder/CrossBeam/koran Newel/Baluster/RailCap/RailMid + EngawaPost) via isDeckPart; re-datums to building floor (wall base, WallPost embeds -0.5, assert loosened 0.6 tol); ROLE/MIRROR trimmed to building parts; FlagMount rehomed PerimF->WallPost at preserved eave height. Re-captured from workspace.TeahousePrototype (Teahouse_04 clone, has current chochin): 0 deck parts, floor y=0, manifest roles={roof,timber,wall}/shojiBays={1,2,3}/hasTatami ok. Gate: building seats flush on T02 procedural deck.
  NOTE (pre-existing, not a regression): readManifest reads FlagMount by TAG but the tool writes untagged Attachments -> manifest.flagMounts always empty; attachPrefab is a stub. Flag mounts functionally dormant.
  DEFERRED: EngawaBarrier (invisible fall collision) kept in building for now; moving it to the pad + deferred pad-collision is a later concern (deck railings are CanCollide=false).
  PLACE-ONLY: teahouse-1story prefab re-captured by hand + workspace.TeahousePrototype (capture source) both persist only if the USER SAVES THE PLACE.

== PHASE 2 COMPLETE (Tasks 4-6 + re-bake; 482c39a, cb8fff8, dc8f30d, 7455efb). Procedural deck geometry done, building prefab slimmed, mountCF re-baked. Deck no longer mirrors -> stray-post + left-hand SideWall bugs gone at the source. 358 Lune green, lint clean. Phase 3 (wire placement + two-input materialization) remains. ==

--- Phase 3 (Tasks 7-12): wire placement + data plumbing. Option A adopted for Task 7 (keep mirror as mechanism, drive from openSide; symmetric-shoji prefab deferred to art). ---
Task 7: complete (commit c27a2fc, 358 green). StructurePlanner Mount.hand->openSide, Plan.mirror->openSide; StructureBuilder mirrors iff openSide=="left" (native prefab = right). TreatmentApplier transitional openSide=spec.hand.
Task 8: complete (commit bf1b909, 358 green). SiteCoordinator Action gains deckSize + teahouse{size,loadout,placement}; scale/sizeClass/footprint kept additive (removed Task 12). Vacant/onLeave = S starter (S deck + dormant S teahouse), NOT maxSize; starterAction() replaces dormantOf.
Task 9: complete (commit dc132f3, data). PadSites: each perch gains deckSize(=maxSize) + placement{offset={0,3},facing="N",openSide}. openSide=left for T03/T05/T06. hand kept transitionally. offset/facing tuned in Task 12.
Task 10: complete (commit 38fd35e, VISUAL GATE PASSED user-confirmed). TreatmentApplier.apply(padId,spec,treatment,deckSize,teahouse?) builds deck always + places building via deckCF*placeCF(placement), scales building by size, non-fatal fit-check, transactional staging swap (F2/F4). main.server injects buildingPlacer+sizeClasses, 4 call sites pass deckSize+teahouse. Gate matrix: T02 claimed-L-right, T05 claimed-L-left, T04/T06 vacant-S-starter; all floors seat 0.00 on deck top.
  NOTE (size check, user-verified): S building == legacy size (it IS the legacy geometry @ scale 1.0); L = sqrt(1.6)~1.27x linear / 1.6x area bigger (top tier; legacy = S base). Progression confirmed intended.
  CLEANUP done: deleted workspace.TeahousePrototype (Task 6 capture source clone was sitting on pad T04 = its legacy home, read as a 2nd teahouse); re-cloneable from CanyonTeahouses on demand. Task10Gate cleared.
Tasks 11-12 REMAIN: bare-deck support (VacantState pure test + applier); full visual gate matrix (all 4 facings, left/right openSide, flat+sloped) + tune PadSites placements + remove dead code (BASE_FOOTPRINT/footprintFor, scale/footprint/sizeClass Action fields, StructureOps.mirrorX if unused, spec.hand).
Task 11: complete (commit 49839f8, 358 green + Studio-verified). VacantState.resolve: claimed pad w/ nil loadout -> BARE DECK (loadout=nil, lit), no forced dormant building. TreatmentApplier builds building only when teahouse~=nil AND teahouse.loadout~=nil. Bare L deck materialized 6 posts + no Structure. Capability (Piece-B owns-deck-no-house), not the vacant default.
Task 12 REMAINS (final): visual gate matrix (bare deck / S-on-L / 4 facings / L-R openSide / flat+sloped T05-T06) + TUNE PadSites placements per-perch + dead-code cleanup (BASE_FOOTPRINT/footprintFor if unused, scale/sizeClass/footprint Action fields, spec.hand; NOTE StructureOps.mirrorX is STILL USED under Option A - keep).
Task 12: complete (commit fef1549, 356 green). Dead-code cleanup: Action dropped scale/sizeClass/footprint; PadSites dropped hand (all 14); main.server log -> deck/teahouse size; PadBuilder.build+PadSpec removed + PadBuilder.spec deleted. KEPT BASE_FOOTPRINT/footprintFor (PadSiteFit) + StructureOps.mirrorX (Option A). Regression gate reproduced the user-approved Task 10 matrix exactly (behavior-preserving). Per-perch placement tuning deferred to later polish (Option A).

== PHASE 3 COMPLETE (Tasks 7-12: c27a2fc, bf1b909, dc132f3, 38fd35e, 49839f8, fef1549). ==
== PIECE A COMPLETE (pad/deck separation, all 3 phases). Deck is now a hand-agnostic procedural pad (S/M/L); building is placed on it via openSide+offset+facing; two-input materialization (deckSize + optional teahouse); bare-deck capable; stray-post + left-hand-mirror bugs gone at the source. 356 Lune green, lint clean. ==
DEFERRED (not blocking): per-perch placement offset/facing tuning; symmetric-shoji prefab (kill mirror, art task); EngawaBarrier fall-collision -> pad; flag-mount tag/attachment mismatch (dormant); M/L authored prefabs vs ScaleTo proxy; Piece B (economy, placement UI, access variable).

FINAL WHOLE-BRANCH REVIEW (Piece A, fb87501..fef1549, opus): merge-ready, 0 Critical.
- Important #1 FIXED (86745bd): TreatmentApplier fault-isolates the whole building step (fit/placeCF/facingYaw were outside pcall -> a bad placement could abort the loop). Now deck-only degrade.
- Minor #2 FIXED: captureTeahouseBase flag-rehome uses world-Y datum delta (was mixing pre/post-strip pivot frames).
- Minor #3 FIXED: dropped vestigial PadBuilder.Mount.hand.
- Minor #4 (test-hygiene: formula-asserting tests) accepted/no-action (plan-mandated).
Reviewer cleared: placeCF yaw matrix, fits extent-swap, transactional staging swap, isDeckPart strip set (EngawaBarrier kept), PadFrame/PadPlanner node parity, footprintFor still used by PadSiteFit.
356 Lune green, lint clean. Branch: 7 unpushed commits.

=== SP1 — Modular Teahouse Foundation (plan 2026-07-12) ===
Base: ba00cab. Phases 1-2 (Tasks 1-4) headless/Lune; Phase 3 (5-8) Studio-interactive.
Task 1: complete (commits 4d9adbe..af16a6c, review clean; 363 Lune green). WallBays pure model (STATES/defaultState/validate/resolve, wholesale invalidation). Fix af16a6c strengthened tests (wholesale + missing-index) per reviewer Important. Minor(carry to final): the inner STATES[chosen] check in resolve is unreachable once the validate gate passes (harmless defensive code).
Task 2: complete (commit c137838, reviewed w/ Task 3; StructurePlanner resolves wallBays -> Plan.bays, drops Plan/Mount.openSide; Manifest.bays required). Implementer added bays={} to all manifest literals incl. StructureBuilder.spec fake.
Task 3: complete (commit 64f304e, review clean; 362 Lune green). StructureBuilder calls ops.applyBays(model, plan.bays), drops mirror; Ops.mirrorX->applyBays. Two mirror tests -> one applyBays order test.
  RESOLVED cross-task finding (reviewer Important): real StructureOps lacks bays/applyBays -> live builds transitionally broken. NOT a gap — Task 7 (Phase 3) updates StructureOps.readManifest(bays)+applyBays; plan's Phase-2 gate documents this ("no Play-test between Task 4 and Task 7").
Task 4: complete (commit 5466ba4, review clean; 362 Lune green). Retired Placement.openSide from BuildingPlacer + TreatmentApplier mount + 14 PadSites entries + SiteCoordinator.spec PLACEMENT. Only surviving openSide = StructurePlanner.spec Task-2 assertion (correct). No placeCF/fits logic change.

== SP1 PHASES 1-2 COMPLETE (Tasks 1-4: 4d9adbe..5466ba4). Pure foundation (WallBays + planner bays) + mirror retired + openSide gone. 362 Lune green, lint clean. ==
RUNTIME is transitionally broken by design (real StructureOps lacks bays/applyBays; prefab has no bay tags) — restored by Phase 3 Tasks 5-7. DO NOT Play-test until Task 7 (per plan Phase-2 gate).
PHASE 3 REMAINS (Tasks 5-8, STUDIO-INTERACTIVE): 5 re-author S walls->bays+door+tatami; 6 capture-tool bay tags + re-capture; 7 StructureOps.applyBays+readManifest.bays, remove mirrorX; 8 full gate + save. Then final whole-branch review.
Minors carried to final review: Task 1 redundant inner STATES[chosen] check.

--- SP1 PHASE 3 (Studio-interactive) ---
Task 5: AWAITING USER VISUAL GATE. Re-authored S walls into 10 bays on workspace.TeahousePrototype (fresh clone of the ServerStorage master; MASTER UNTOUCHED, Task 6 re-captures from proto). Built via execute_luau in local frame (pivot -190.69,218.75,94; look +Z-ish).
  BAY CONTRACT (for Task 6 capture + Task 7 applyBays):
  - 10 Models `Bay_<side>_<index>`, each with child Models `Solid` / `Shoji` / `Door`.
    * Solid = 1 Panel (Role_wall, Wood 96,74,52; front/back 5.8x10x0.4, sides 0.4x10x5.8, centerY=5).
    * Shoji = Shoji panel (Role_wall, SmoothPlastic 232,222,196 trans .45) + ShojiGlow (Neon 127,103,70 trans 1.0, no-collide) + 4 Mull horiz bars (Wood 74,55,38) at Y={2.10,3.80,5.50,7.20}. Matches legacy front shoji.
    * Door = 2 Jamb + 1 Lintel (Role_timber, Wood 74,55,38) around a passable ~4w x ~7h gap (no panel).
  - EVERY authored part carries attribute `ShownTransparency` (= its natural transparency). Task 7 applyBays: SHOW -> Transparency=ShownTransparency, CanCollide=true; HIDE -> Transparency=1, CanCollide=false.
  - Default authored visibility: front bays show Shoji; all others show Solid; Door always hidden. (== today minus mirror; right side now SOLID, was 3 uneven shoji.)
  - INDEX ORDER = left-to-right seen from OUTSIDE: front idx1 X=-6..idx3 X=+6; back idx1 X=+6..idx3 X=-6; left idx1 Z=+3.32,idx2 Z=-2.68; right idx1 Z=-2.68,idx2 Z=+3.32. (cz front=-5.68 back=+6.32; cx sides=-/+9; pitch 6.)
  - Regularized the non-uniform +X side (was 3 panels @ ~4 pitch) to 2 uniform 6-stud bays symmetric with left.
  - Tatami: proto.TatamiMats = 12 `Tatami` mats (5.4x0.2x2.5 Fabric 150,140,96, 3col x 4row, pitch 5.6/2.7) + 1 dark TatamiUnderlay (seams). Replaces the single 17x11 Tatami.
  - Kept: 4 corner WallPost, roof/rafters/gable/skirt/plate/moss, ChochinSwing/Hook, EngawaBarriers, Table, InteriorGlow.
  BUILD BUG caught+fixed in-attempt: shownVariant passed lowercase vs capitalized model names -> all variants hidden (walls invisible); fixed by case-correct show pass.
  NOT SAVED. Gate shots: front (3 shoji ok), rear-corner (back 3 solid bays + sides 2 solid). Awaiting user OK before Task 6.

Task 5 REDESIGN (user feedback: "shoji are sliding screens, not post-framed panels; they must actually work"). User chose (AskUserQuestion) "Real screens, state-driven open" over live-animation / model-rethink.
  Shoji now authentic sliding screens (NOT one framed panel/bay):
  - hashira POSTS stay at bay boundaries only (structural ken grid: front/back X=+-3/+-9, sides Z=+-5.68/0.32/+6.32) -> delimits the addressable bays. NO timber between the 2 screens of a bay.
  - per shoji-capable bay: kamoi (grooved head rail) + shikii (sill) timber tracks spanning the 6-stud opening; 2 thin translucent screens (SmoothPlastic 232,222,196 trans .45) each with kumiko lattice (3 horiz + 1 vert bar, 120,96,66 Wood) on PARALLEL grooves (inner gi=+0.09*inward / outer go=-0.07*inward).
  - Shoji variant = screens CLOSED (L screen uc-1.35 outer, R screen uc+1.35 inner; overlap ~0.3 centre; cover ~5.7). Has a ShojiGlow neon plane (trans 1.0) for night-light.
  - Door variant = same screens SLID OPEN (both stacked at uc+1.35 on the 2 grooves) -> left ~2.7 clear/passable. So door == shoji-open (state-driven, no live tween). SP1 keeps 3 states solid/shoji/door.
  - Part counts/bay: Solid=1, Shoji=13, Door=12. All 10 bays uniform.
  - Solid panels butt (6.1 in 6-slot); tatami butted mats + dark underlay (unchanged from prior fix).
  Gate shots: front closed (kumiko grid + tracks + hashira only at bay lines), door-demo (front_2 slid open, left half passable) then reverted. AWAITING USER OK.

Task 5 CONVERGED design (after 2 more user corrections):
  - "posts between front bays are wrong": REMOVED all interior hashira. Only the 4 CORNER posts remain. Each wall is one continuous opening.
  - Per wall: continuous kamoi (head rail) + shikii (sill), TIMBER 74,55,38, span corner-to-corner (front/back 18, sides 12), building-level frame (always present).
  - Per bay: ONE full-bay-width framed screen (~5.9 wide) on ITS OWN groove (depth offset per bay index: front -.13/0/+.13, back mirror, sides +-.11) so screens slide the full wall + stack. NOT 2 half-screens.
  - Each screen = EXTERNAL FRAME (2 Stile + 2 Rail) + internal Kumiko (2 vert + 3 horiz) + Shoji paper (SmoothPlastic 232,222,196 trans .45, tag Role_wall) [+ ShojiGlow neon on closed]. Frame+Kumiko = BLACK wood 35,33,37 (user pref) -- untagged so recolor won't touch.
  - Shoji variant = screen at home (covers bay). Door variant = same screen slid one bay aside (park = home +-5.9, last bay slides -) -> home slot open/passable. Solid variant = 6.1 solid panel.
  - Part counts/bay: Solid=1, Shoji=11 (4 frame+5 kumiko+paper+glow), Door=10. + per-wall Kamoi/Shikii (8) + 4 corner posts + tatami.
  - Default: front Shoji, back/sides Solid, Door hidden. GrooveDepth attr on each Bay for Task 7.
  Gate: front reads as 3 framed black-lattice shoji, corner posts only, continuous rails. AWAITING final user OK before Task 6.

Task 5 REFINEMENTS (more user feedback, all applied):
  - Grooves: user "don't need 3 grooves on the back if only one door" -> visible GrooveRail track (4 rails on shikii+kamoi) is FRONT-ONLY (track=true front). Back/sides = plain solid wall + plain Kamoi/Shikii beams; a door there pockets. Screen groove DEPTHS still per-bay (for pocketing) but no visible multi-track on non-front walls.
  - Tatami HERI: each mat = inset field (5.35x2.45 Fabric 150,140,96, tag Tatami) + 4 Heri border strips (grey-brown 100,90,74 Fabric) -> mats read individually (was reading as one field when butted). 12 mats + underlay.
  - Kumiko UNIFORM grid: was wide centre column (2 vert bars). Now 3 vert (u +-1.475/0) + 4 horiz (Y 1.95/3.55/5.15/6.75) = even ~1.48x1.6 cells across all screens.
  - Rear door confirmed: back_2->Door pockets screen into adjacent solid wall (tobukuro), clean passable opening; user "pocketed shoji is fine for now".
  - Screens flush to floor (bottom rail Y~0), 6.2 wide (overlap at meeting stiles, no gaps), grooves sep 0.2 > thk 0.16 (any screen slides to any bay).
  - Frame+kumiko BLACK 35,33,37; paper cream trans .45 tag Role_wall; per-bay GrooveDepth attr. Total prefab ~478 parts.
  Studio left with front_2 open (tatami peek). AWAITING final OK -> then Task 6 (tag bays + re-capture).

Task 5: COMPLETE (VISUAL GATE PASSED, user "good to go"). Converged sliding-shoji S teahouse on workspace.TeahousePrototype (place-only; MASTER prefab still old until Task 6 re-captures). 10 bays Solid/Shoji/Door, front-only 3-groove track, uniform kumiko, tatami heri+seam. Default front-shoji/rest-solid. ~478 parts. Place NOT yet saved.

Task 6: COMPLETE (tool rewritten + prefab re-captured; commit pending). captureTeahouseBase.luau REWRITTEN for SP1 bays: dropped deck-strip/Perim-datum/mirror machinery; now clones bay-structured floor-datumed proto + tags Bay(+Side/Index/GrooveDepth) on 10 models, Role_wall/roof/timber + Tatami by name, ShojiBay+Bay on FRONT shoji only, 2 FlagMount attachments (clears inherited dupes first), asserts 10 bays + floor y~=0. Re-ran: Bay=10, Role_wall=30/roof=6/timber=38, ShojiBay=3(1,2,3), Tatami=12, MirrorX=0, FlagMount=2, 478 parts. stylua clean. PLACE MUST BE SAVED (prefab place-only). Then Task 7 (StructureOps.applyBays+readManifest.bays, remove mirrorX).

Task 7: COMPLETE (VISUAL GATE PASSED, place saved; commit pending). StructureOps: readManifest now returns bays (10 from Bay tags + Side/Index); applyBays(model,bays) shows chosen variant per bay, hides others (restore ShownTransparency, CanCollide iff visible so ShojiGlow stays non-collide; hidden = transp1/no-collide; missing bay/state/variant warns+skips, never blanks -> F2/F4); mirrorX REMOVED (no external consumer). stylua+selene clean, 362 Lune green. Gate matrix through REAL clonePrefab/readManifest/resolve/applyBays: default (front shoji/rest solid), rear door (back_2 passable), both-sides shoji (no mirror) all correct. BayGate throwaway removed.

Task 8: COMPLETE (verification-only, no commit). grep src mirrorX/openSide = CLEAN; 362 Lune green; stylua+selene clean. BayGate throwaway removed. End-to-end matrix validated in Task 7 (real clonePrefab/readManifest/resolve/applyBays: default, rear-door, both-sides-shoji). T02/T05 hand distinction moot (mirror fully retired, placement hand-agnostic since Piece A). Door passability verified via collision (pocketed screen leaves slot empty; hidden variants non-collide). Pre-existing canyon artifacts (Sandbox drafts, cyan PadDeck previews) left as-is (referenced by PadSites; not SP1 scope).
== SP1 COMPLETE (Tasks 1-8). Modular bay teahouse: WallBays model + planner bays + builder applyBays + mirror retired + bay-tagged prefab + StructureOps.applyBays/readManifest.bays. Sliding shoji (3 framed screens/wall on continuous tracks), pocket doors, heri tatami. 362 Lune green. Ready for final whole-branch review. ==

FINAL WHOLE-BRANCH REVIEW (SP1, ba00cab..9e0a72c, opus): MERGE-READY, 0 Critical, 0 Important, 2 Minor + 1 observation.
- Minor 2 FIXED (this commit): WallBays.validate threw on a non-list side value (e.g. {front="shoji"}), aborting the caller -> violated F2/F4 "never abort". Added type(states)~="table" guard + 2 tests (364 green). Low reachability (upstream type-check) but now self-defensive.
- Minor 1 OPEN (accepted/follow-up): WallBays.resolve silently discards an invalid map with no warn (spec F2/F4 says "must warn"). Pure Lune module cannot call Roblox warn; warning would need a fallback-signal return threaded to a Roblox caller. Fallback is behaviorally safe. Left as documented limitation.
- OBSERVATION (follow-up, outside SP1 diff): server/src/loadout.ts LOADOUT_KEYS lacks "wallBays" -> validateLoadout rejects any loadout carrying wallBays, so wallBays is always nil at runtime and SP1 renders all-defaults until the whitelist is extended (SP2/Piece B wiring).

=== Per-Size Deck Placement (plan 2026-07-13) ===
Base: 61a41cb. Phase 1 (Tasks 1-3) headless/Lune; Phase 2 (Task 4) Studio-interactive (with user).
Task 1: complete (commit 336751e, review clean; 367 Lune green). DeckPlacement.resolve(deckPlacements,deckSize,maxSize) pure lookup + fallback + nil.
Task 2: complete (commit f1ec19c, review clean; 367 Lune green). SiteCoordinator CENTERED={offset={0,0},facing="N"} in starterAction+onJoin; spec helper dropped mountCF/placement; assertion toBe->toEqual.
  Minor(carry to final): only onJoin asserts teahouse.placement==CENTERED; starterAction/vacant path unasserted for placement (behavior verified by grep, path exercised by onLeave/vacant tests).
Task 3: complete (commit 2e60a80, review clean; 367 Lune green, selene+stylua clean). PadSites mountCF->deckPlacements={L=..} (14 entries, values byte-identical) + dropped placement; TreatmentApplier resolves per-size deck pivot via DeckPlacement.resolve (nil-check BEFORE staging -> no orphan; commit loop intact) + _buildBuilding(spec->deckCF12); main.server requires+injects DeckPlacement.
  Minor(carry to final): DeckPlacement.resolve (Task 1 module) doesn't guard deckPlacements==nil -> would error if a PadSites entry omitted deckPlacements entirely (moot now; all 14 populate it). F2/F4 defensive guard when next touched.
== PHASE 1 COMPLETE (Tasks 1-3: 336751e, f1ec19c, 2e60a80). Pure DeckPlacement lookup + centered teahouse + per-size PadSites/TreatmentApplier wiring. 367 Lune green, lint clean. Only L authored -> S/M fall back to L until Phase 2 survey. ==
PHASE 2 REMAINS (Task 4, STUDIO-INTERACTIVE w/ user): survey S/M deck placements per perch (surveyDeckPlacements.luau place/bake) + paste into PadSites + visual gate + save place. Then final whole-branch review.
Task 4: complete (commit 69d8ff3, VISUAL SURVEY user-confirmed "finalize"; 367 Lune green, stylua clean). Surveyed S/M/L deck placements for all 14 perches via draggable-slab flow (surveyDeckPlacements.luau place/bake); user dragged each deck to touch access + face view; rotation+deck-top-Y locked to L; T01 realigned. PadSites now deckPlacements={S,M,L} x14. Centered L-teahouse walk-around eyeballed (T05 = the tight door-case for Piece B). DeckSurvey cleared, PadRefs restored; place unchanged (values in git, not place-only) so no save needed.
== PHASE 2 COMPLETE (Task 4: 69d8ff3). == ALL TASKS COMPLETE. Final whole-branch review next.

FINAL WHOLE-BRANCH REVIEW (per-size deck placement, 61a41cb..69d8ff3, opus): MERGE-READY, 0 Critical/Important/Minor surviving. Verified 367 Lune green; all 14 PadSites entries author S/M/L valid 12-arrays w/ per-perch yaw-consistent rotation + monotonic S->L toward view; F2/F4 nil path warns+returns before staging (commit loop intact); SiteCoordinator CENTERED in both paths, no spec.placement/spec.mountCF reads remain; _buildBuilding spec->deckCF12 clean; main.server wired; surveyDeckPlacements bake locks rot+Y to L correctly. 2 carried Minors confirmed moot (all specs populate deckPlacements; only structure specs reach resolve). NO FIX WAVE NEEDED.
== PER-SIZE DECK PLACEMENT COMPLETE (Tasks 1-4: 336751e, f1ec19c, 2e60a80, 69d8ff3). Merge-ready. ==

---

# SDD Progress — B1 Player Back Door (Piece B, sub-project 1)

Plan: docs/superpowers/plans/2026-07-13-b1-player-back-door.md
Base: 37be480

Tasks 1-3 = unit-tested (T1 Vitest, T2/T3 Lune). Tasks 4-5 = Roblox-runtime glue (no Lune tests by design; lint/analyze + visual gate). T5 visual gate is user-interactive (stop-and-ask).
Task 1: complete (commit d1851df, review clean; 21 Vitest passing). validateWallBays + wallBays whitelist. No Critical/Important.
  Minor(carry to final): no test asserting length==MAX_BAYS_PER_SIDE (8) is ACCEPTED (only +1 rejected); low risk.
Task 2: complete (commit 6e51964, review clean; 375 Lune green +8, stylua+selene clean). BackDoorEditor set/move/toggle/clear; table.clone (shallow but string-only, safe). No Critical/Important.
  Minor(carry to final): docstring says "deep-copied" but table.clone is shallow (safe only because side values are strings); no test asserting post-call structural independence of out.front vs input.front.
Task 3: complete (commit d461282, review clean; 377 Lune green +2, stylua+selene clean). NetworkClient.setTeahouse PUT mirrors setPreferences. Footer verified on d1851df/6e51964/d461282. No Critical/Important.
  Note(non-blocking): task-3-report.md has unrelated deck-placement text appended (report hygiene only; shipped code clean).
Task 4: complete (commit a8fd05e, review clean; stylua+selene clean, 377 Lune green no regress). Server handler occupant-only from playerHouse stash; manifest-derived backBayCount; live applyBays; best-effort persist; stash set-on-claim/cleared-both-leave-paths; 2 remotes declared. table.clone(loadout) safe (wallBays replaced wholesale). No Critical/Important.
  Minor(carry to final): float bayIndex in 1..N passes range-check -> setBackDoor matches nothing -> silently CLEARS the player's own door instead of ignoring (add bayIndex % 1 == 0 guard). Also: malformed stashed wallBays (non-table) would throw inside setBackDoor loop (engine-contained per-fire; write-validated; defense-in-depth only).
Task 5: complete (CODE ONLY; commit 4b1fdc8, review clean; stylua+selene clean, 377 Lune green no regress). BackDoorController client: per-bay prompts, ^Bay_back_(%d+)$ enumeration, dedup+relabel, WaitForChild, fires {bayIndex}; removed unused player/Players locals. Visual gate (Step 4) DEFERRED to main session w/ user. No Critical/Important.
  Minor(carry to final): backDoorIndex not type-guarded in OnClientEvent (trusted server; cosmetic-only). Informational: no ChildAdded re-scan for late-streaming bays (server materializes synchronously; watch in visual gate).

FINAL whole-feature review (37be480..4b1fdc8, opus): READY TO MERGE = YES. No Critical/Important. End-to-end verified: wire contract, persistence round-trip, live re-render, F2/F4 all coherent (server 21/21, Roblox 377/377).
Fix wave (one subagent): land Minor #1 (float bayIndex guard: reject non-integer) + #2 (BackDoorEditor docstring "deep-copied"->accurate) + #4 (Node boundary-accept test len==8). DEFER #3 (non-table stashed wallBays; write-validated, engine-contained) + #5 (client backDoorIndex type-guard; trusted server, cosmetic) as documented defense-in-depth.
Fix wave: complete (commit 61ab128; vitest 22/22, lune 377/377, stylua+selene clean; diff = exactly the 3 scoped changes). #3/#5 deferred as documented.

== B1 CODE COMPLETE (37be480..61ab128). Merge-ready per final review. REMAINING: interactive Studio visual gate (T4+T5 end-to-end) with user; then user decides push/merge (branch merges to main are the user's call). NOT pushed. ==

== B1 VISUAL GATE PASSED (user-confirmed 2026-07-13) & DEPLOYED ==
Deploy: pushed branch -> dev App Runner (roshambo_server_dev, auto-deploy) now runs B1 server code; wallBays PUT 400->200 confirmed. Root cause of "persist failed" was the stale deployed backend, NOT B1 code.
Gate confirmed: persistence across sessions works; doors slide correctly; prompts positioned right; back open.
Follow-up fixes (all pushed 61ab128..5fdb4a2; place saved for prefab geometry):
  55aad4d prompt logic: once a door exists, only the door bay prompts (others silent) until removed
  3f95641 PadBuilder: leave deck back edge OPEN (drop back railing; back beam stays)
  #2 (place-state, SAVED): reflected Bay_back_1/3 Door variants to pocket toward CENTER (were sliding outward over the deck)
  f5501ff BackDoorController: anchor prompts to the fixed Solid panel (bay centre), not the sliding Door screen
  5fdb4a2 back-door prompt key E->F (avoid collision with Favorite's E on the same teahouse)
Surfaced (NOT built): teahouse access-control/invitation system -> memory teahouse-access-control-backlog (its own future sub-project).
Spawn note: players spawn at DevChannelSpawn (dev spawn), NOT at their teahouse; teahouse builds out at the claimed perch. No spawn-at-teahouse mechanic (by design for now).

---

# SDD Progress — B2 Teahouse Size Economy (Piece B, sub-project 2)

Plan: docs/superpowers/plans/2026-07-13-b2-teahouse-size-economy.md
Base: a952620

Pre-flight fix (pre-execution): plan Task 8 owner prompt now alternates deck<->teahouse (was deck-only -> teahouses were unbuyable via UI). Committed in the plan.
Tasks 1-6 unit-tested (T1/T2/T3 Vitest, T4/T5/T6 Lune). T7-T8 runtime glue (lint + visual gate). T3 is SERVER code -> needs push/deploy before the T8 Studio gate.
Task 1: complete (commit 45886c6, review clean 0 findings; 11 Vitest, tsc clean). economy.ts pure validate/applyPurchase, linear ladders, teahouse<=deck, exact prices/errors, non-mutating.
Task 2: complete (commit 359c630, review clean; 5 Vitest, DB round-trip). User.maxDeckSize enum incl null, default null. Footers verified 45886c6/359c630.
Task 3: complete (commit e963036, review clean; 130 Vitest, RED-before, footer verified). GET /economy superset + POST /purchase via pure economy.ts; atomic save; teahouse .set() mirrors existing PUT. Implementer fixed a brief bug (missing DEFAULT_TEAHOUSE_LOADOUT import). No Critical/Important.
  Minor(carry to final): teahouse-purchase test checks POST response not a GET-reread persistence (deck test does reread); no dedicated 401 test for the 2 new routes.
Task 4: complete (commit 81a52f9, review clean 0 findings; 383 Lune, lint clean). SizeClasses minSize/nextTier/resolveBuilt; teahouse capped by BUILT deck (perch-cap subtlety correct).
Task 5: complete (commit 65762b3, review clean; 386 Lune RED-before, lint clean). onJoin(playerId, economy{maxDeckSize,teahouses}, prefs); deck-ownership eligibility; bare-deck for deck-only; sizes from resolveBuilt. Rewritten "size stays primary" test VERIFIED legitimate (old per-tier premise structurally gone). NOTE: main.server.luau:428 still on OLD onJoin signature -> Task 7 fixes it (transitionally broken by design; Lune green since main.server has no tests). No Critical/Important.
  Minor(carry to final): stale test title "a preferred site is claimed first within the fitting size tier" not renamed (behavior correct).
Task 6: complete (commit 0d9b01a, review clean 0 findings; 389 Lune, lint clean, footer verified). NetworkClient getEconomy + postPurchase mirror existing methods.
Task 7: complete (commits b35fbbb..914c978, re-review Approved; 392 Lune incl 3 new isVacant tests, lint clean, footers verified). Server wiring: Occupied attr, RequestPurchase/EconomyState remotes, playerEconomy stash, getEconomy join switch (old onJoin call replaced), purchase handler. FIXED Important #1: buy-to-claim now pre-checks pad vacancy (refuse+no-purchase if not vacant), post-purchase claims the SPECIFIC pad (action.padId==padId guard), non-silent warn on race, no fall-through. Added SiteCoordinator:isVacant (tested).
  Minors(carry to final): (#2) Occupied attr not reset for garden/valley vacantForm (DEAD today - all 14 pads dormant-structure); (#3) teahouse loadout stashed as hardcoded {baseStyle} vs postPurchase result (matches server DEFAULT_TEAHOUSE_LOADOUT); (#4) upgrade branch fires on any successful purchase (always a size purchase in B2); (dup) two string.match over item in the handler.
Task 8: complete (CODE ONLY; commits 7ffc28d..dc237ff, re-review Approved; 392 Lune, lint clean). EconomyController walk-up buy/upgrade prompts, alternating deck<->teahouse offer. FIXED 2 Criticals + 1 Important: anchor to PadDeck (bare decks have no Structure); rebind on rebuild (track anchor instance identity); react to ChildAdded+Occupied per site (other players' claims). Visual gate (Step 4) DEFERRED to main session w/ user.
  Minor(carry to final): unfiltered ChildAdded on site folders -> harmless recreate churn during rebuild (could filter to PadDeck/Structure names like PerchPreferenceController).

== B2 ALL 8 TASKS CODE COMPLETE (a952620..dc237ff). REMAINING: final whole-feature review; then push/deploy (Task 3 server routes) + interactive Studio visual gate w/ user. ==

FINAL whole-feature review (a952620..dc237ff, opus): READY TO MERGE = WITH FIXES. Round-trip verified end-to-end (IDs, payloads, onJoin signature, buy-to-claim race all coherent). 1 Important FIXED (commit 4e7e986): purchase handler now re-checks player presence after the postPurchase yield (was a permanent pad leak on leave-mid-buy-to-claim; mirrors the join guard). 6 carried Minors all non-blocking (T3 test depth/401; T5 stale title; T7 garden Occupied dead-code; T7 hardcoded teahouse loadout==server default; T7 upgrade-on-any + dup match; T8 ChildAdded churn).
Optional non-blocking follow-ups (deferred): set Occupied=false in apply's garden branch; source granted teahouse loadout from postPurchase response before B3.

== B2 CODE COMPLETE & MERGE-READY (a952620..4e7e986, 11 commits). REMAINING: push/deploy (Task 3 server routes) + interactive Studio visual gate w/ user. NOT pushed. ==

== B2 VISUAL GATE PASSED (user-confirmed 2026-07-13) & DEPLOYED ==
Deploy: pushed branch -> dev App Runner now runs B2 routes (GET /economy 404->200 confirmed). Test account seeded (robloxId 11060590099: 10000 pts, maxDeckSize=null, teahouses={} -> clean non-owner via a mongoose script).
Gate confirmed: full ladder S->M->L walked (buy-to-claim S deck -> S teahouse -> M deck/teahouse -> L deck/teahouse; balance 10000->4120; each purchase persists + live-rebuilds). Owner auto-claim + biggest-owned build confirmed looks right.
Cleanup fixes (pushed 4e7e986..7f71616; place-state moves need a place SAVE):
  5cafe10 PadOps: pad posts top at the beam underside (buried 0.1), not the deck datum (z-fight)
  7f71616 TreatmentApplier.shutter: only darken SHOWN shoji, not hidden-variant shoji (dormant 'phantom black walls' on every bay; NOT the barrier - coincident location)
  place-state (SAVE to persist): TeahousePrototype moved Workspace -> ServerStorage.AuthoringSources; Sandbox.PadRefs (84 parts) hidden
Spawn note (unchanged): players spawn at DevChannelSpawn, not their teahouse (owner auto-claims a perch on join, preference-first -> T06).

== B2 COMPLETE, VALIDATED, DEPLOYED (a952620..7f71616). NEXT in Piece B: B3 customization UI (fold Favorite+door+economy prompts into one teahouse menu; retire E/F/G key split) or B4 repositioning. ==

---

# SDD Progress — Teahouse Size Tiers (authored per-size prefabs S/M/L)

Plan: docs/superpowers/plans/2026-07-13-teahouse-size-tiers.md
Base: b449be5

Tasks 1-4 = code (subagent, Lune; T4 runtime glue no unit test). Tasks 5-8 = INTERACTIVE Studio authoring (main session w/ user: re-designate M shell-only, author S 2x1, author L 4x3, combined gate + orphan cleanup). Code lands first (clones teahouse-1story-{s,m,l}, retires scale), then prefabs authored, then integrate at gate. Runtime transitionally broken between T5 and T8 (by design).
Task 1: complete (commit 07a3aee, review clean 0 findings; 393 Lune, lint clean). SizeClasses.prefabName = "{style}-{size:lower()}".
Task 2: complete (commit 30aeb19, review clean 0 findings; 396 Lune, lint clean). SizeClasses.buildingFootprint per-size wall footprints (S/M/L half-extents); M matches old BUILDING_BASE.
Task 3: complete (commit 2345358, review clean 0 findings; 396 Lune, lint clean). StructureBuilder.build gains prefabName arg (clones it, not loadout.baseStyle); rest unchanged. TreatmentApplier still on OLD signature -> Task 4 fixes (transitionally broken; runtime-only, Lune green).
Task 4: complete (commit ae96c12, review clean 0 findings; 396 Lune, lint clean). TreatmentApplier: buildingFootprint fit-check, prefabName resolution + passed to build (5-arg), ScaleTo REMOVED, deck scale/deckFootprint untouched, fault-isolation intact.

== CODE PHASE DONE (Tasks 1-4, b449be5..ae96c12). Runtime clones teahouse-1story-{s,m,l}, scale retired. NOW: interactive Studio authoring (Tasks 5-8) w/ user. Runtime transitionally broken until prefabs authored + gate. ==
Task 5 (M re-designate): STRUCTURAL strip DONE (teahouse-1story-m created; removed Koran/WEndRail/EngawaSupport folders + 12 Skirt/SkirtEnd; kept 10 bays + hip roof + tatami + shoji track + chochin). Verified structurally; on-deck visual (no double railing) folds into the Task 8 gate. Temp look-copy at workspace._SizeTierPreview.M_preview (0,400,0).
Tasks 6-7 (author S 2x1 + L 4x3): USER-DRIVEN in Studio (reshape is rotated ~17deg + coupled bays/track/tatami -> blind scripting too risky). I do DETERMINISTIC SUPPORT on request: duplicate prefab, delete named parts/bays, re-index bay names + Index attrs (contiguous 1..N per side), measure/verify footprints, apply/verify tags (Bay/Side/Index/Role/ShownTransparency). NOT started (awaiting user Studio work).
Task 5 CORRECTIONS (Studio, this session):
  (a) Spec strip-list error: Skirt/SkirtEnd are the ROOF eave-skirt (Y~227, Slate), NOT an engawa apron. My strip wrongly removed all 12 -> restored from the original into -m. Only Koran/WEndRail/EngawaSupport should have been stripped. S/L authored from M keep the skirt.
  (b) Left-door handedness bug (inherited from shipped teahouse-1story): Bay_left_1/left_2 Door (open) variant slid OUTWARD past the corners into space (z 9.3 / -8.7) instead of inward toward the neighbor like the right bays. Fixed by reflecting each left Door about its closed-panel plane (pure building-Z translation, dz -12.1 / +12): left_1 open 9.3->-2.7, left_2 open -8.7->3.3. Right bays were already correct.
  USER DECISION: leave the original teahouse-1story unfixed (it's deleted at Task 8; -m replaces it).
  NOTE: -m fixes are place-only (ServerStorage.StructurePrefabs) -> persist ONLY when the user saves the place.
Task 6 (author S 2x1) IN PROGRESS (Studio, workspace._SizeTierPreview.S_work @ 60,400,0):
  - Staged from -m; dropped surplus bays (front_3, back_3, left_2, right_2) -> 6 bays.
  - Snapped 6 bays to exact 2x1 grid (building-local): front_1(-3,-3) front_2(3,-3) back_2(-3,3) back_1(3,3) left_1(-6,0) right_1(6,0). 12x6 footprint. Kept bays already contiguous-indexed (no re-index needed).
  - DESIGN RULE (user): S side bays are SOLID-ONLY. Single-bay side wall = no door slot AND no room for a shoji to slide. Removed Door+Shoji variants from left_1/right_1 (Solid only, shown). Back-door feature is back-wall-only anyway.
  - REMAINING (user drives roof; I do deterministic track/tatami/posts on request): hip roof re-proportion to 12x6, shoji track (kamoi/shikii/grooverail) shrink, tatami shrink, wall posts reposition, chochin reposition. THEN measure footprint -> SizeClasses.buildingFootprint("S").
  - place-only: persists ONLY on user save.
Task 6 S ROOF (from-scratch build) DONE (workspace._SizeTierPreview.S_work.RoofNew):
  - Transform-scaled M roof was a mess at S scale -> user approved a from-scratch clean parametric hip (proper style, uniform 30.5deg pitch, ridge = width-depth).
  - CALIBRATION NOTE (reusable): Roblox WedgePart has its full-height vertical face at +Z, ramp descending toward -Z, right-angle at bottom-+Z. Wedge-from-triangle helper: P0=right-angle -> local(-sy/2,+sz/2), +Y leg=P1, -Z leg=P2 => uY=(P1-P0).Unit, uZ=-(P2-P0).Unit, uX=uY:Cross(uZ), C=P0+uY*L1/2-uZ*L2/2, CFrame.fromMatrix(C,uX,uY,uZ), Size(thick,L1,L2). (First build flipped the Z-leg -> splayed "death metal" roof.)
  - Hip = ridge + 2 central slabs + 4 corner wedges + 4 hip-end wedges. Then per reference (traditional chashitsu): deep flared eave (overhang ~4, eaveX=10/eaveZ=7, eave drops to y8.83), exposed rafter tails (taruki) under all 4 eaves, 4 hip rafters, fascia boards, + triangular gable vent (wood frame + dark recess) on the front slope at the ridge. 53+5 parts.
  - REMAINING for S: nudge chochin under the new deeper eave; final walk-around; SAVE PLACE (place-only). Then author L (4x3) with the same builders.
Task 6 S COMPLETE + STAMPED (2026-07-14): teahouse-1story-s in ServerStorage.StructurePrefabs.
  - 2x1 shell (solid-only side bays, fixed door handedness), 2x2 standard tatami, centered table, shrunk track/posts.
  - IRIMOYA ROOF (from-scratch): clean hip -> deep flared eaves (overhang 4) -> irimoya (front+back gable ends w/ bargeboard frame + dark tympanum vent, 2 connecting slopes, upper Z-ridge). Removed fake taruki; lifted shell 0.3 to hide hip girders. Chochin tucked to eave underside (top y9.5).
  - Pivot at floor datum (pivotY-floorY=0, like -m) -> seats identically. RoofNew flattened into prefab top-level. User saved the place.
  - CLEANUP TODO before ship: remove workspace._SizeTierPreview (M_preview + S_work) - stray floating teahouses at y400. Keep for now as L authoring reference.
Task 7 L (4x3) NEXT: stage from -m (3x2), ADD 1 bay/wall (front/back 3->4, sides 2->3 = +4 bays, 14 total), snap to 4x3 grid (x=-9,-3,3,9 / z=-9,+9 front/back; x=+-12 / z=-6,0,6 sides; footprint 24x18), scale track/tatami/posts, irimoya roof via same builders scaled up, VERIFY fit on L deck (trim to 4x2 if cramped), stamp teahouse-1story-l.
Task 7 L COMPLETE + STAMPED (2026-07-14): teahouse-1story-l in ServerStorage.StructurePrefabs.
  - 4x3 shell (14 bays, +4 cloned from -m + re-indexed to M convention: front/back idx+ =+X/-X, left/right idx+ =-Z/+Z), door handedness fixed (4 end bays reflected inward), scaled track/posts, 4x6 standard tatami, centered table.
  - CLEAN LOW HIP roof (NO gablet, per user ref image): pitch lowered 30.5deg->16.6deg (k=0.30), ridge y13.9, eave y10.0, footprint eave x+-16/z+-13 (~deck edge). Same wedge builder. No taruki, no hip rafters. Fascia. Chochin moved OUT to (14,-11.5) toward front-right corner, fully sheltered under eave (verified underRoof).
  - Pivot at floor datum, RoofNew flattened. All 3 sizes now stamped: teahouse-1story-{s,m,l}.
FINISH LINE (remaining):
  1. Combined Studio GATE: Play, walk S/M/L on their decks - verify build/seat/koran/doors/dormant-shows-S. (user-driven)
  2. Delete orphan bare `teahouse-1story` (retired; nothing clones it - prefabName appends size). 
  3. Clean up workspace._SizeTierPreview (M_preview/S_work/L_work stray at y400) before final save.
  4. Final code review of tier code tasks 1-4 (deferred).
  5. Save + publish place.

=== TEAHOUSE SIZE TIERS: COMPLETE (2026-07-14) ===
- Code tasks 1-4 committed (07a3aee..ae96c12), final review = READY TO MERGE (no Critical/Important).
- S/M/L prefabs authored + stamped (teahouse-1story-{s,m,l}), orphan deleted, scratch cleared.
- Combined gate PASSED. Place SAVED + published by user.
- BUILDING_HALF left as review found it (gate-validated conservative placeholders; refinement declined). Optional future: nudge to exact authored measurements + stale docstring at TreatmentApplier.luau:19.

=== B3 TEAHOUSE MANAGEMENT UI + DISPLAY SIZE (started 2026-07-16, plan a3324fc) ===
Task 1 (B3 validateDisplay): complete (commits a3324fc..15cbb21, review clean/Approved). Minors for final review: (a) no test asserts display==owned equality boundary; (b) teahouse<=displayedDeck cross-field invariant deliberately NOT in validateDisplay — Task 3 resolveBuilt clamps it at build time (plan-confirmed).
Task 2 (B3 display persistence): complete (commits 15cbb21..54ed189, review Approved). Reviewer Important (plan-mandated, controller-RESOLVED): POST /display full-replace semantics — omitted field == explicit null reset; OK because Task 6 client always sends BOTH fields (plan Task 6 Step 3 "change only that dimension, keep the other"); FINAL REVIEW should re-confirm the shipped client does send both. Minors for final review: no route-level BAD_DISPLAY test; no GET round-trip test of persisted non-null displays.
Task 3 (B3 resolveBuilt display caps): complete (commits 54ed189..c41cc8e, review Approved). Minors for final review: stale resolveBuilt doc comment (no mention of display caps/"none"); minSize errors if deckDisplay=="none" ever leaks (upstream validateDisplay forbids it); no combined-both-displays test, no teahouseDisplay>owned test.
Task 4 (B3 TeahouseMenuModel): complete (commits c41cc8e..c041794 incl. fix wave, re-review Approved). Fix wave 1: locked teahouse rung leaked price/affordable (plan example code bug) -> gated on effective buyable + regression test; "next=highest owned+1" kept (B2 ladders strictly linear, comment now documents contiguity). Minors for final review: SIZES hand-duplicates reverse of SizeClasses.order; no defensive nil-check for unrecognized size strings in rankOf.
Task 5 (B3 SetDisplay wiring): complete (commits c041794..ca669ef incl. fix wave, re-review Approved). Implementer corrected the plan's res.body -> res.data (Result type has no body; plan snippet would crash). Fix wave 1: buy-to-claim onJoin economy table now threads deckDisplay/teahouseDisplay (reachable: owner joins during pad scarcity then buys once a pad frees). Minor for final review: SiteCoordinator.luau:1-12 header comment omits the new display clamp. NOTE: rojo serve restart needed before Studio play (new SetDisplay remote).
Task 6 (B3 TeahouseController HUD panel): complete (commits ca669ef..8ce1ecc, review Approved; 485-line new client file). All remote contracts verified against server source (incl. SetDisplay full-replace + Lua nil-omission convergence -> both-fields rule holds). Startup-safe (catalogReady guard, Active=false defaults). Gate-labeled Minors: favorites show raw siteIds (PadSites.displayName is server-only); pre-echo "Loading" window shows bare S/M/L; toggle/panel position vs existing HUD unverified. VISUAL GATE still pending (combined with Task 7).
Task 7 (B3 EconomyController trim): complete (commits 8ce1ecc..515c0bb, review Approved). offerFor returns nil,nil for ALL owners; buy-to-claim path byte-for-byte intact (server contract re-verified). Minor: trailing return nil,nil covers two cases without a comment. All 7 B3 code tasks DONE -> final whole-branch review next, then combined Task 6+7 USER VISUAL GATE (rojo serve restart required first: new SetDisplay remote).
FINAL WHOLE-BRANCH REVIEW (B3, a3324fc..515c0bb, fable): verdict "With fixes".
- Important 1 (REAL BUG, FIXED e04efee): playerHouse[uid] stale after live rebuilds -> back-door edits mispersist onto wrong size's loadout; fixed at ALL THREE sites (SetDisplay rebuild, upgrade rebuild, buy-to-claim -- last one also stale, fixer caught it); nil'd when built teahouse is nil. Verification by final reviewer pending.
- Important 2 (HUMAN DECISION, pending at gate): spec says validate/menu-gate teahouse<=DISPLAYED deck; shipped = independent prefs validated vs owned only, effective clamp at build time (world always correct; panel can show teahouse 'L' selected while world shows S). Options: amend spec (+ 'effective' indicator later) OR add cross-check to validateDisplay + TeahouseMenuModel teaOpts gating.
- Gate-wave bundle (Minors 3-6,8 + T6 gate items): silent SetDisplay gate-fail (no echo; claimless owner sees live display rows + upgrade rung server won't honor); no no-op/in-flight guard on SetDisplay (HTTP+rebuild on same-value click); rapid cross-dimension clicks lose first change (echo self-corrects); post-purchase invisibility hint (owned>displayed); panel fixed 340x520 non-scrolling vs spec "scrolling" (32 favorites overflow) + button top-right vs spec bottom-corner; favorites raw siteIds; pre-echo bare S/M/L window.
- Triage accepts (no action): T1 equality-boundary test; T2 route BAD_DISPLAY/GET round-trip tests; T3 "none"-leak + extra Luau tests; T4 SIZES dup + rankOf guard; T7 comment.
- Recommendation logged: shared per-player HTTP-in-flight guard for the 4 free remotes (B-series hardening, future).
FINAL-REVIEW FIX VERIFIED (e04efee, reviewer confirmed-fixed): all 3 playerHouse sync sites correct (join-shape mirrored, loadout provenance checked), nil-teahouse path correct, no new yield/presence hazard, doc comments accurate. Lune 407 green + lint clean re-run at HEAD.
New non-blocking minors -> gate wave: (a) buy-to-claim sync rationale comment factually wrong (teahouse-before-deck impossible via validatePurchase; branch's teahouse arm only reachable via crafted payload from returning unclaimed owner -- code right, fix comment); (b) PRE-EXISTING: SetBackDoor never writes edited loadout back to e.teahouses[house.size] -> later same-session rebuilds re-render pre-edit loadout (DB correct, rejoin heals); follow-up: e.teahouses[house.size] = newLoadout in SetBackDoor handler.

=== B3 CODE COMPLETE (2026-07-16, a3324fc..e04efee, 10 commits, NOT pushed) ===
Server 141 Vitest green; roblox 407 Lune green; stylua+selene clean. Final review verdict: ready to merge WITH the visual-gate wave.
PENDING (user):
  1. DECISION (final-review Important 2): spec says validate teahouse<=DISPLAYED deck at POST + shrink menu options; shipped = independent prefs (validated vs owned), effective clamp at build (world always right; panel can show teahouse L selected while world shows S; raising deck display restores teahouse). Amend spec OR add cross-check to validateDisplay + teaOpts gating.
  2. PUSH branch (dev App Runner must serve POST /display before the gate, like B1).
  3. Restart rojo serve (new SetDisplay remote), then combined Task 6+7 VISUAL GATE (plan Task 6 step 6 + Task 7 step 4).
  4. Gate wave bundles: doc/UX minors listed above + panel scroll/position + favorites names + pre-echo window + silent gate-fail echo + no-op SetDisplay guard + post-purchase hint + wrong buy-to-claim comment.
2026-07-16 (later): USER DECIDED Important 2 -> KEEP SHIPPED SEMANTICS; spec amended (1a0ff25: build-time clamp not validation-time, menu options independent). Branch PUSHED 7f71616..1a0ff25 (size-tiers + B3 + amendment) -> roblox-ci + dev App Runner redeploy. Awaiting POST /display live, then user visual gate.
GATE WAVE 1 (2026-07-16, from user gate feedback): back-door F picker never appeared after mid-session teahouse acquisition (fresh-account walk). ROOT CAUSE (2 layers): BackDoorState fired ONLY at join + SetBackDoor echo, never at the 3 rebuild sites; AND client bindBays dedupe held destroyed prompt refs after rebuilds (applier destroys old Structure incl. prompts). FIXED ba9b905 (echoBackDoor helper at all 3 sites + client destroyed-prompt sweep), reviewed confirmed-correct. User gate otherwise: "everything else felt good". Account reset performed for the fresh walk (robloxId 11060590099 -> 10000 pts, cleared; dev db). Latent minor logged: echoBackDoor relies on contiguous-ownership invariant (validatePurchase, TS-side) — defensive guard if tier-skipping ever lands. RETEST pending: mid-session display toggle / purchase -> F prompts on new structure.
GATE WAVE 2 (2026-07-16, fix ba9b905 INSUFFICIENT — user repro: door built in M vanished on deck:L upgrade rebuild, still no prompt): systematic-debugging round 2. EVIDENCE: DB HAS the door (teahouses.M.wallBays.back=[solid,solid,door]) -> rebuild rendered from stale e.teahouses stash = the known deferred SetBackDoor write-back gap, now user-proven. Prompt failure = event-time binding races async replication (RemoteEvents NOT ordered vs instance ops; TreatmentApplier keeps folder stable but swaps children). FIX c7a9214 (reviewed confirmed-correct by fable, 8-scenario walk): (a) SetBackDoor mirrors newLoadout into e.teahouses[house.size]; (b) BackDoorController REWRITTEN geometry-driven — persistent folder watch (ChildAdded Structure -> bindStructure w/ instance-identity cache reset + late-bay watch), remote only carries padId/activeIndex. Minors logged: nil-anchor bay permanently skipped (unreachable under Persistent streaming + Deferred signals); orphaned prompts on mid-session pad change (no such path today). User state note: owns S/M/L teahouses + L deck, displays L/L; door lives on the M loadout (per-size) -> visible when teahouse display = M. RETEST pending.
BACK-DOOR RETEST PASSED (user-confirmed) — c7a9214 closes the saga. POLISH WAVE SHIPPED 85b3eb0 (reviewed, all 8 items spec-compliant, 417 Lune green incl. 10 new TDD cases): A claimless-owner gating (vm.claimed/needsClaim; mutually exclusive with non-owner notice); B deckShownSmaller/teahouseShownSmaller + panel hint; C PreferenceState names map (all 3 fire sites) -> real favorite names; D favorites ScrollingFrame (140px viewport, derived); E ready/Loading pre-echo state; F no-op display click guard (vs desired); G desired-pair fires SetDisplay (echo-reconciled, no lost update; silent server path unreachable via UI thanks to A); H buy-to-claim comment corrected. Review minors (accepted): combined shown-smaller hint doesn't name the dimension; selection highlight lags until echo. Remaining decision parked: HUD button top-right (spec said bottom-corner) — user seemed happy, left as-is. FINAL polish visual check pending.
Button reposition 329cc04 (user request): HUD toggle top-right -> bottom-right (points/leaderboard UI owns top-right per Roblox convention); panel opens upward (anchor 1,1; bottom 60px above screen bottom). Matches the spec's original bottom-corner call. Final visual check pending (polish wave + button spot together).

=== B3 COMPLETE & SHIPPED (2026-07-16) ===
Final visual check PASSED (user). Place SAVED (user). Pushed 1a0ff25..329cc04 (gate fixes ba9b905+c7a9214, polish 85b3eb0, button 329cc04); full B3 range a3324fc..329cc04 all on origin. Server 141 Vitest + roblox 417 Lune green. Branch m4b-zendojo-art-pass continues (long-running; merge to main = user's call). Next Piece B candidates: B4 teahouse repositioning, spawn-at-teahouse, access control (see backlog memories).

=== B4 TEAHOUSE REPOSITIONING (started 2026-07-16 after B3 ship; spec 962053e amended 9c2b2bd, plan 9c2b2bd) ===
B4 Task 1 (BuildingPlacer.clamp): complete (commits 9c2b2bd..6da78d2, review Approved). Minors for final review: yaw-swap block duplicated between fits and clamp (plan-mandated; a rotatedHalfExtents helper would dedupe); no exact-fit (half*2 == deck width) boundary test (hand-traced correct via clamp(v,x,x)).
B4 Task 2 (validatePlacement + whitelist): complete (commits 6da78d2..0b8b2fe incl. boundary-test fix wave, re-review Approved). Minors for final review: boundary test hardcodes 32 instead of importing MAX_PLACEMENT_OFFSET; untested-but-traced-correct rejects (string-number offsets, missing facing, missing offset). Note: BAD_PLACEMENT wins over LOADOUT_TOO_LARGE on dual failure (hook precedes byte check; fine, documented).
B4 Task 3 (TeahouseMenuModel.canMove): complete (commits 0b8b2fe..ce95ab4, review Approved, no findings above Minor: canMove/needsClaim mutual exclusion verified but not jointly asserted; test-1 naming inherits brief wording). 426 Lune green.
B4 Task 4 (server wiring): complete (commits ce95ab4..df38191 incl. nil-guard fix wave, re-review Approved). Fix wave: SetPlacement guards e.teahouses[size]==nil before clone (plan-mandated gap; unreachable via shipped economy -- contiguous ladders -- but real for manual DB edits/tier-skip grants). Minors for final review: no echoEconomy on SetPlacement success (INTENTIONAL: placement not in EconomyState; re-examine with Task 6 client in view); SiteCoordinator test reuses "p1" as player+pad id; guard could sit before the clamp call (wasted pure math on the dead path). 427 Lune green.
B4 Task 5 (EventBus + Move button): complete (commits df38191..1691906, review Approved, zero findings). Gating chain verified airtight (pre-ready/canMove-false/claimedPadId-nil cannot fire); idioms mirror renderDisplayOptions.
B4 Task 6 (MoveController): complete (commits 1691906..155236d, review Approved). Minors for final review/gate: (1) drag raycast Include-filters whole site folder -> can hit faded original's walls/roof (plan-inherent; placement still clamped correct; GATE judges feel — if annoying, filter to deck parts only); (2) HUD button connections not in conns (ScreenGui destroy auto-disconnects; comment-worthy); (3) humanoid.Died only wired if humanoid exists at enter. facingFromPivot<->placeCF yaw convention independently verified exact (incl. E/90deg gimbal case). ALL 6 B4 CODE TASKS DONE -> final whole-branch review, then USER VISUAL GATE (rojo serve restart required: new SetPlacement remote).
B4 FINAL WHOLE-BRANCH REVIEW (962053e..155236d, fable): verdict "With fixes". Architecture verified sound (shared clamp idempotent across client/commit/render; pivot round-trip exact incl. W/S yaw cases; all 8 apply-callers covered; no TS<->Luau drift -- max legal clamped offset ~16.44 << 32; B3 geometry-watch integration clean).
- Important 1+2 (FIXED 6f84d2a, verification pending): SetPlacement pre-yield clone could clobber mid-yield SetBackDoor edit (loadout newly multi-writer) + pre-yield resolveBuilt could rebuild stale sizes after mid-yield SetDisplay. Fix: post-yield placement-only merge onto CURRENT stash + post-yield resolveBuilt recheck (skip rebuild if size no longer standing) + MoveController suppresses original's enabled prompts during mode (tracked, restored on cancel; preserves relabel state).
- Minors ledgered: no SetPlacement rate limit (spam = HTTP+rebuild each; shared debounce w/ family fix); Move button dead-clicks silently when active/attrs-missing; rotate logic duplicated (HUD + R key); RaycastParams alloc per frame.
- BACKLOG (B-series hardening): per-uid serialization for the 4 mutating handlers (SetPlacement/SetBackDoor/SetDisplay/RequestPurchase) + shared debounce -- retires the mid-yield race family the project keeps paying per-instance fixes into.
- Triage accepts: T1 yaw-dup + exact-fit test; T2 hardcoded-32 + untested rejects + error precedence; T3 joint assertion; T4 no-echoEconomy-on-success (verified safe: EconomyState carries no loadouts; client reads world attrs) + p1 id reuse + guard-after-clamp; T6 HUD conns + humanoid.Died coverage. Gate item: raycast can hit faded original (if drag feels bad, exclude structure -> ray falls to deck).
B4 FINAL-FIX VERIFIED (6f84d2a, reviewer: READY TO MERGE): placement-only merge correct (no stale-field resurrection; mid-yield door edits survive in stash+world+echo); builtNow recheck covers display-none/deck-shrink/mid-yield-upgrade/spam interleavings; single suspension point unchanged; prompt suppress/restore round-trips both relabel regimes. Residuals ledgered: raced-PUT DB one-field staleness (heals next write/rejoin; cite in per-uid serialization backlog); two sub-second client re-arm windows.

=== B4 CODE COMPLETE (2026-07-16, 962053e..6f84d2a, 12 commits incl. docs, NOT pushed) ===
Server 148 Vitest + roblox 427 Lune green; lint clean. Final review verdict: ready to merge pending the visual gate.
PENDING (user): (1) PUSH branch -- the gate REQUIRES it: SetPlacement persists via PUT /teahouses which 400s UNKNOWN_KEY until the dev App Runner runs Task 2's placement whitelist (same stale-backend trap as B1/B3); (2) restart rojo serve (new SetPlacement remote); (3) VISUAL GATE (plan Task 6 Step 8): ghost drag/snap/rotate/commit/cancel, per-size memory, clamp-on-shrink, back-door prompts re-arm + suppressed during mode; judge raycast drag feel over the original building.
B4 GATE WAVE 1 (2026-07-16, user gate feedback): (a) teahouse flush to railed edge -> railing bars visible inside walls; (b) far-corner drags awkward (occlusion + edge-miss). FIX 9d7a738: SizeClasses.placementBounds (deckFootprint inset RAIL_CLEARANCE=1.0, derived: 0.45 deepest rail inboard face + 0.5 clearance) at all 3 clamp sites; stepDrag = deck-PLANE intersection (aim past corner -> clamp parks in corner). Review of 9d7a738 found a CRITICAL regression: L teahouse E/W-rotated needs 24.4 vs L inset depth 23.30 -> degenerate-center + fits-fail -> building VANISHES on commit (pre-fix it "fit" with 0.449 margin = inside the 0.45 rail = was never actually legal). FIX 2 d59df04 (432 Lune green): pure BuildingPlacer.canFitFacing + size-x-facing test matrix (L/E+L/W sole illegal combos); client rotate() extracted (dedupe Minor closed) + skips unfitting facings; server rejects unfitting facing pre-clamp; TreatmentApplier falls back to facing N (re-clamp+fits) before deck-only skip. User DB checked: both persisted placements facing N (no live data on the vanish path). Re-review pending. User gate result otherwise: move works, per-size persistence confirmed.
B4 GATE FIXES RE-REVIEWED confirmed-correct (6f84d2a..d59df04): canFitFacing boundary semantics consistent with clamp/fits trio; rotate() bounded + can never land on unfitting facing (L-on-L = N<->S toggle); server reject pre-clamp/pre-persist; render fallback safe on nil-loadout paths (pcall isolation intact); commit 1 undisturbed. Minors ledgered: silent server reject (only reachable by bypassed client); no cue that rotate skipped 2 facings (gate judges). RE-GATE pending: railing clearance at flush park, far-corner drag feel (plane intersection), L rotate = N/S only. All Roblox-side — no push needed for this re-test (rojo serves the working tree).

=== B4 COMPLETE & GATED (2026-07-16) ===
Re-gate PASSED (user: "working much better") — railing clearance, plane-intersection drag, N/S-only L rotation all good. Full range 962053e..d59df04 (14 commits); through 6f84d2a pushed, gate fixes 9d7a738+d59df04 LOCAL pending push. Server 148 Vitest + roblox 432 Lune green. NO place save needed (B4 is pure code; folder attributes are runtime-stamped). Backlog carried: per-uid handler serialization + shared debounce (mid-yield race family; raced-PUT one-field DB residual); silent server-reject UX; rotate-skip cue.
B4 PUSHED complete: 6f84d2a..d59df04 up; origin == local == d59df04. B4 fully shipped 2026-07-16.

=== PER-UID HANDLER SERIALIZATION (started 2026-07-19; spec ce7c34d, plan cebce49) ===
Task 1 (HandlerQueue module + 5 Lune tests): complete (commits cebce49..2bd63be, review Approved, state machine hand-traced clean: one-worker-per-lane, identity-guarded retirement, live-pending cap, clear-mid-flight). 437 Lune green. Minors for final review: retirement check's `#lane.pending == 0` is vacuous (loop just exited, no yield between) — comment/remove; untested contrived reentrant-clear-during-own-flight (two logical workers, but identity guard prevents same-lane double-drain; not the HTTP-thunk usage pattern).
Task 2 (wire 6 handlers + clear + no-op skips): complete (commits 2bd63be..439a068, review Approved zero findings). All 6 wrapped verbatim + correctly nested (bodies read in full, not grep-trusted); BankRequest task.spawn REPLACED; clear in economy PlayerRemoving; both no-op skips exact + nil-safe; 3 stylua reflows (applier:apply + 2 resolveBuilt) verified argument-identical. 437 Lune green + rojo build OK + lint clean. Both code tasks done -> final whole-branch review.
FINAL WHOLE-BRANCH REVIEW (ce7c34d..439a068, opus): READY TO MERGE, zero Critical/Important. Serialization invariant correct + tested; scope exact (SubmitPick/FateResolved correctly excluded, no HTTP); clear+rejoin identity guard sound; B3/B4 per-instance body fixes now redundant-but-harmless (untouched); only HTTP yields inside thunks (echo/pushStats/fireProfile synchronous). Minors: (1) hung HTTP blocks that uid's whole lane (bounded by RequestAsync timeout + NetworkClient 3-retry; scoped to one player) -> DOCUMENTED; (2) overflow drop doesn't resync the dropped request (accept, spec's drop-newest); (3) PlayerAdded join not on lane (pre-existing, out of scope); (4) clear test only covered deferred worker -> ADDED clear-during-flight test. POLISH 0f37e2d (both cheap minors): clear-during-flight Lune test (438 green, passed first run) + block-window header note. Triage-accepts: vacuous #pending==0 on retire; contrived reentrant-clear.

=== SERIALIZATION COMPLETE (2026-07-19, ce7c34d..0f37e2d, 4 commits, UNPUSHED) ===
438 Lune green + rojo build OK + lint clean. Pure Roblox-side; no server/PWA change; NO visual gate (fix only observable under self-contention; Lune tests are the verification). NO place save needed. Retires the mid-yield race family the B3/B4 reviews kept flagging + closes the same-player raced-PUT DB residual.
SERIALIZATION PUSHED: d59df04..0f37e2d up; origin == local == 0f37e2d. Shipped 2026-07-19.

=== HOME PORTAL MVP (started 2026-07-19; spec 13a6cb9, plan f2620aa) ===
Task 1 (economy portal item + portalOwned): complete (commits f2620aa..ebed22f, review Approved). Branch order verified (NEEDS_DECK->ALREADY_OWNED->points); portalOwned preserved through non-portal purchases (hand-traced). 153 Vitest green. Minors for final review: no regression test that a deck/teahouse buy preserves a pre-existing portalOwned (code correct, untested); no NEEDS_DECK-vs-ALREADY_OWNED tie-break test.
Task 2 (User.portalOwned + routes echo): complete (commits ebed22f..94f13e4, review Approved). readEconomy/GET/purchase all echo portalOwned; DB round-trip test; teahouse branch untouched for portal. 155 Vitest green. Minor: no test that a portal buy leaves user.teahouses untouched (guaranteed by kind!=='teahouse'). SERVER SIDE COMPLETE.
Task 3 (PortalTarget pure resolver): complete (commits 94f13e4..5b09477, review Approved, row-major rotation math hand-traced correct for identity + 90deg yaw). 442 Lune green. Minor: the wz test assertion is near-vacuous alone (offset dx=0) but the wx assertion still catches a row/col transpose.
Task 4 (thread portalOwned + build deck control): complete (commits 5b09477..d7ee718, review Approved zero findings). All 5 owner apply sites thread portalOwned (join 535, buy-to-claim 717, upgrade 781, SetDisplay 850, SetPlacement 965); 3 vacant/release sites correctly skip (492/528/571). Control gate on treatment.portalOwned==true (fires for bare deck too), staged pre-swap, pcall-isolated, tagged PortalControl + padId attr. 442 Lune green + rojo build OK.
Task 5 (portal buy row + view-model): complete (commits d7ee718..197f54a, review Approved zero findings). vm.portal canBuy = claimed AND ownsDeck AND !owned AND affordable; button mirrors moveButton idiom (LayoutOrder 73), fires {item="portal"} only when currentPortalCanBuy, hidden without a deck; catalogReady gate + econ defaults prevent pre-echo nil. 446 Lune green. Minor: catalogReady checks only catalog.deck not .portal (legacy-echo cosmetic "nil pts", not a crash; Task 4 guarantees portal in catalog).
Task 6 (PortalController + spawn cleanup): commits 197f54a..71c65bd. Spec-compliant (arena Touched = own home + portalOwned + claim; deck prompt NO owner-check = public exit; pure PivotTo no-DB; deckCFForClaim multi-return captured both ends; DevChannelSpawn sweep). Review found Important: shared-boolean _openThresholds guard desynced across open->use->reopen (stale 12s timer zeroed flag under a live newer episode -> two live thresholds). FIXED 71c65bd: guard keyed by threshold INSTANCE; both closers check ==threshold; timeout body fully guarded. Re-review pending. Accepted Minors: brief omitted `workspace` from DI (uses global; Roblox-only); deckPlacements[built.deckSize] no DeckPlacement.resolve fallback (all PadSites have S/M/L today). Renamed unused opener->_opener for selene. 446 Lune green + rojo build OK.
Task 6 RE-REVIEW Approved (197f54a..71c65bd): instance-keyed guard closes the two-live window (open->use->reopen->stale-timer traced), no double-Destroy/Disconnect, no orphan, surgical diff. ALL 6 PORTAL CODE TASKS DONE -> final whole-branch review, then USER VISUAL GATE (push first: purchase persists via /purchase, stale-backend trap; then rojo restart).
FINAL WHOLE-BRANCH REVIEW (13a6cb9..71c65bd, opus): READY WITH FIXES, zero Critical/Important. Economy branch + threading + teleports correct end-to-end; teleport reads atomic under HandlerQueue; deck size resolved fresh at teleport; SetBackDoor does in-place applyBays (never rebuilds -> control survives trivially). PRE-GATE FIX beed8b3 (2 lines, verified exact): deckCFForClaim uses DeckPlacement.resolve (was raw index -> silent no-op for unauthored size; DeckPlacement already required, sig matched); control build also gates on treatment.lit==true (defensive). Ledgered accept/gate minors: T1/T2/T3 tests (accept, correct-by-construction); catalogReady ignores catalog.portal (cosmetic "nil pts", gate); ARENA_PORTAL_CF/ARENA_LANDING absolute placeholders (VERIFY vs real ArenaSpawn pos at gate or portal buries/floats); PortalController uses `workspace` global not DI (cosmetic). GATE WATCH: server PivotTo to a distant not-yet-streamed deck -> +3 Y offset headroom, watch for a fall on first hop.

=== HOME PORTAL CODE COMPLETE (2026-07-19, 13a6cb9..beed8b3, 9 commits, UNPUSHED) ===
Server 155 Vitest + roblox 446 Lune green + rojo build OK + lint clean. Final review: ready for visual gate + merge (fixes landed).
PENDING (user): (1) PUSH (purchase persists via /purchase -> stale dev backend 400s UNKNOWN_ITEM/portal until redeploy; same trap as B1/B3/B4); (2) restart rojo serve (new PortalController/PortalTarget modules); (3) VISUAL GATE (plan Task 6 Step 9): buy Home Portal in panel -> control appears on deck railing + survives display/size/move rebuild; open control -> threshold -> arena; walk arena portal -> own deck; non-owner no-op at arena but CAN use a deck control; spawn at ArenaSpawn (no DevChannelSpawn). TUNE at gate: ARENA_PORTAL_CF/ARENA_LANDING vs real ArenaSpawn; DECK_LANDING_LOCAL per-size; control railing spot; placeholder portal art (art pass later).
HOME PORTAL GATE WAVE (2026-07-19, commit c20a822, from user gate feedback, NOT pushed): (1) ARENA_PORTAL_CF -> lower overlook railing (79.9,109.3,9.3, user-surveyed); (2) ARENA_LANDING -> ArenaSpawn (30,116.7,8) — was 4 studs from portal -> bounced arrivals straight home; land at spawn instead. (3) Deck control: size-aware (deckFootprint) placement against a side railing toward back; AUTO-SWAP to lateral half away from the B4-movable teahouse (teaDx>0 -> left else right); yaw 90 flush/parallel to railing facing deck interior. (4) Open ANIMATES: marker tweens INTO the portal (0.25s grow, base-anchored) and shrinks back; instance-keyed guard preserved. (5) STAYS OPEN as shared doorway (deck->arena public), closes 5s-from-last-use (os.clock deadline + reschedule; was 12s close-on-first-use); per-player touch debounce. 446 Lune green + rojo build + lint clean. Gate item still open: control/portal HEIGHTS + arena portal art vs terrain (placeholder). NOT YET RE-REVIEWED (gate polish); fold into final-review re-check before merge.
GATE WAVE REVIEWED Approved (beed8b3..c20a822): close-lifecycle (5s idle, single pending task, post-closing no-op), instance guard (no double-open regression), per-player debounce bounded, auto-swap sign sound (clamp never crosses zero), rotation preserved through tween, wide axis parallel to railing, landing ~50 studs clear of trigger. Minor (ledger): touch handler's `if not closing` guard is unreachable (touchConn disconnected before closing set) — harmless/fail-closed, comment nit. PUSHED 0f37e2d..c20a822 -> origin. HOME PORTAL fully on origin.

=== DECK DECORATION FRAMEWORK (started 2026-07-19; spec 37e8e12, plan 258d3ce) ===
11 tasks. Base for Task 1 = 258d3ce (plan commit). Buy-per-placement props, ghost-drag, auto-hide.
Task 1 (economy decoration pricing/cap/id): complete (commits 258d3ce..8f82156, review clean, Spec OK + Approved). 34 Vitest green. Minor for final review: applyPurchase's `next` literal drops deckDecorationCount — BY DESIGN (readEconomy recomputes it from user.deckDecorations.length each call; validation-only read, not persisted through applyPurchase). No action.
Task 2 (validateDecorations): complete (commits 8f82156..8702079, review clean, Spec OK + Approved). 39 Vitest green. Minor for final review: no test for a non-object array entry (code rejects it correctly; regression-unprotected branch).
Task 3 (User schema + apiV1 routes): complete (commits 8702079..c6e3119, review clean zero findings). 175 Vitest green + npm run build clean. SERVER SIDE COMPLETE (purchase decoration branch early-returns; id minted server-side via appendDecoration; PUT /decorations gated by validateDecorations).
Task 4 (BuildingPlacer.footprintBounds): complete (commits c6e3119..81d92a8, review clean, Spec OK + Approved). 448 Lune green + lint clean. Minor for final review: third copy of the halfX/halfZ yaw-swap idiom (pre-existing pattern across fits/canFitFacing/footprintBounds; extract-a-helper opportunity, not introduced here).
Task 5 (DecorationCatalog): complete (commits 81d92a8..dbeec97, review clean, Spec OK + Approved). 453 Lune green + lint clean. IMPORTANT FIX: implementer moved the STONE/WOOD/WATER/LEAF Color3 constants from module-scope (as the plan sample had them) INTO the builder bodies — module-level Color3.fromRGB would have broken require() under Lune, and thus Task 6 (DecorationLayout requires DecorationCatalog). Now Lune-safe. Minors: report said "no deviations" (inaccurate — this was a deviation); cosmetic STONE/WOOD dup across builders.
Task 6 (DecorationLayout.resolve): complete (commits dbeec97..88fb642, review clean zero findings, Spec OK + Approved). 459 Lune green + lint clean (reviewer re-ran both). Strict-inequality overlap confirmed (edge-touch visible); clamped offset emitted; unknown propId skipped; input not mutated. PURE MODULE LAYER (T4/T5/T6) COMPLETE.
Task 7 (TreatmentApplier decorations): complete (commits 88fb642..39da376, review clean zero findings, Spec OK + Approved). 459 Lune green + rojo build OK + lint clean. _buildBuilding returns Placement/nil (one real skip path, not two as brief prose said — reconciled); builtPlacement captured; decoration step between building & portal-control blocks; portal block + transactional commit intact; pcall fault-isolated; deps threaded in applier + main.server .new (4-line main.server change only, no stash/handler/remote touch yet). No-op at runtime until Task 8 sets treatment.deckDecorations.
Task 8 (server wiring: remotes/stash/handlers/DecorationPlaced): complete (commits 39da376..1fc9b61, 2 commits inc. fix, review Approved after fix). 459 Lune green + rojo build OK + lint clean. CRITICAL CAUGHT+FIXED (plan-originated bug, 1fc9b61): decoration-buy branch ran AFTER `e.maxDeckSize=res.data.maxDeckSize`/`e.portalOwned=...`, but the /purchase decoration response omits those -> buying a decoration nulled e.maxDeckSize -> resolveBuilt short-circuits -> ALL later rebuilds + portal control silently break for the session. Fix: detect decoration branch BEFORE those adoption lines; decoration branch sets only totalPoints+deckDecorations (decorations never change tier/portal). rebuildClaimedPad defined after applier (line 503), all 5 treatment sites threaded, both handlers re-check IsDescendantOf post-yield, server-authoritative clamp (deckFootprint), stale-id no-ops, HandlerQueue per-uid. LESSON for final review: any early-return purchase branch that reuses res.data must not sit below unconditional adoptions of fields its response omits.
Task 9 (generalize MoveController): complete (commits 1fc9b61..fd963ce, review clean zero findings, Spec OK + Approved). 459 Lune green + rojo build OK + lint clean. TEAHOUSE PATH BEHAVIOR-EQUIVALENT (reviewer verified step-for-step vs old enter): placementBounds (not deckFootprint), Structure:Clone() ghost (live never mutated), fade/prompt-disable/restore, facingFromPivotOf(structure,m) pre-startMove, same drag/rotate/exit hooks, commit->SetPlacement identical payload. Decoration entry: deckFootprint (full deck), DecorationCatalog.build/footprint nil-checked, commit->SetDecorationPlacement{id,offset,facing}. commitFn set-per-move + cleared in exit (no cross-move leak). EventBus gained MoveDecoration.
Task 10 (panel Decorations section + view-model): complete (commits fd963ce..36b7ae9, review clean, Spec OK + Approved). 464 Lune green + rojo build OK + lint clean. vm.decorations ordered by DecorationCatalog.ids(); canBuy = claimed AND ownsDeck AND underCap AND affordable AND price~=nil (all 5 conjuncts; each verified non-vacuous). ensureDecorButtons defined before render (fwd-ref OK). Deviations (both behavior-preserving, validated): (1) "no deck" test assigns st.maxDeckSize=nil AFTER construction — Lua table-constructor nil-elision would've made it vacuous otherwise; (2) UDim2.new(1,0,0,0)->UDim2.fromScale(1,0) for a selene warning.
Task 11 (DecorationController + propId attr): complete (commits 36b7ae9..873c705, review clean zero findings, Spec OK + Approved). 464 Lune green + rojo build OK + lint clean. Tag-watch (GetInstanceAddedSignal/RemovedSignal "Decoration") + rescanAll on EconomyState + DecorationPlaced; tryBind single funnel (ownership filter padId==claimedPadId; bound dedup; pendingAutoMoveId fired-once-then-cleared); Move(E)->MoveDecoration, Remove(X)->SetDecorationRemove; anchors PrimaryPart. Applier _buildDecorations now sets propId attribute (REQUIRED — else Move dead). Deviation: dropped brief's unused DecorationCatalog require (selene clean gate). ALL 11 TASKS DONE -> final whole-branch review next.

FINAL WHOLE-FEATURE REVIEW (258d3ce..873c705, opus): READY WITH FIXES, one Critical. Server/validation/persistence/id-round-trip/catalog-parity/frame-consistency/rebuild-threading/concurrency all verified clean; teahouse-move/portal/back-door/display unregressed; the 1fc9b61 fix confirmed + no sibling pattern.
CRITICAL (seam between auto-hide T6/7 and auto-place/prompt T11): buy mints prop at [0,0]=deck centre; on a teahouse-owning deck the teahouse is also centred -> DecorationLayout hides it -> _buildDecorations skips -> no tagged instance -> buy auto-move (waited for instance) never fires -> points+cap-slot spent on an invisible, unreachable prop. Drag-under-house / grow-over hidden-but-persisted is BY DESIGN per spec (overlaps allowed, shrink-to-reveal, user-confirmed) -> NOT changed.
FINAL FIX (addbf16): decouple buy auto-move from the built instance (realizes the spec's stated "buy -> ghost-drag place"): DecorationPlaced carries propId; DecorationController fires MoveDecoration{part=nil} immediately; MoveController.enterDecoration tolerates nil part (folder from padId, ghost from catalog, start at stash default) and startMove guards all descriptor.original steps; SetDecorationPlacement operates on the stash so commit works for a never-rendered prop. enterTeahouse untouched (behavior-equivalent). RE-REVIEW: RESOLVED (reviewer re-ran 464 Lune + rojo + lint himself). +0d2fc9a stale-header comment fix.
DECK DECORATION FRAMEWORK CODE COMPLETE (2026-07-19, 258d3ce..0d2fc9a, 14 commits, UNPUSHED). 175 Vitest + 464 Lune green + rojo build OK + lint clean.
PENDING (user): (1) DEPLOY/redeploy the server before the gate — decoration purchases go through PWA /purchase which now handles decoration:* ; a stale dev backend 400s them (BAD_ITEM), same trap as B1/B3/B4/portal. Use the local docker stack OR redeploy. (2) restart rojo serve (new DecorationCatalog/DecorationLayout modules + DecorationController + generalized MoveController). (3) VISUAL GATE (plan's post-Task-11 checklist): panel Decorations section lists 4 props w/ prices; buy -> immediate ghost-drag; 4 distinct footprints; in-world Move/Remove (owner-only); AUTO-HIDE (place under small teahouse display, grow -> vanish, shrink -> return); cap at 24; props survive display/size/move rebuilds; TEAHOUSE MOVE still works (regression). RESIDUAL UX NOTE for the gate: cancelling the buy ghost-drag leaves the prop at the centred default, which is auto-hidden under a centred teahouse -> it's managed by shrinking the teahouse to reveal its prompt (shrink-to-manage, per spec). Watch whether that feels acceptable or wants a visible default slot later. Minors carried (accept): applyPurchase drops deckDecorationCount (by design); no non-object-entry decoration test; 3rd yaw-swap copy; DecorationCatalog color dup.
NOTE: one server test (Atlas-backed DB round-trip) is intermittently flaky under load (timeout); clean re-run = 175/175 green, exit 0. Environmental, not a code regression.

=== TEAHOUSE ACCESS CONTROL — QUEUED, NOT STARTED (2026-07-19; spec fbb9826, plan 07a3b14) ===
10 tasks. Base for Task 1 = 07a3b14 (plan commit). User opted to /compact before executing. RESUME: superpowers:subagent-driven-development on docs/superpowers/plans/2026-07-19-teahouse-access-control.md. Same TS↔Luau patterns as the decoration framework (economy/loadout/User/apiV1 + pure Luau + main.server stash/handlers/HandlerQueue + client controller/panel). Watch seams: per-viewer AccessBlocked/AccessState contracts, client-local-collision (workspace-parented client parts), friend/eviction timing, the decoration-style "buy branch below unconditional adoption" class of bug does not apply here but the post-yield IsDescendantOf recheck discipline does.

--- TEAHOUSE ACCESS CONTROL EXECUTION (base 07a3b14) ---
Task 1 (backend access model + validateAccess): complete (commits 07a3b14..deec13c, review clean, Spec ✅ + Approved). 86 Vitest green (13 new). validateAccess rejects non-object/array, unknown mode, extra keys, non-array invited, non-int/non-positive/dup id, over-cap(51). Minors for final review (both no-action): (1) exact-50 boundary untested (only 51 tested); (2) Check.error typed plain string (pre-existing codebase pattern).
Task 2 (User schema + apiV1 access routes): complete (commits deec13c..64d9c75, review clean, Spec ✅ + Approved). 188 Vitest green (no regressions) + npm run build clean. Schema subdoc + GET /economy field + PUT /players/:id/access (resolve->500 RESOLVE_FAILED / validate->400 no partial write / replace whole payload). No username handling server-side (confirmed). No route unit tests BY DESIGN (no route suite in repo). BACKEND COMPLETE.
Task 3 (AccessPolicy.canEnter pure): complete (commits 64d9c75..b241713, review clean, Spec ✅ + Approved). 469 Lune green (5 new) + lint clean. Pure (no Roblox datatypes); owner-first short-circuit, public->true, friends->isFriend, private->invited-contains, unknown->false(fail-closed). Minor for final review (inherited from brief, no-action): unknown-mode branch implemented but untested.
Task 4 (AccessGates geometry): complete (commits b241713..7154062, review clean, Spec ✅ + Approved). 472 Lune green (3 new) + lint clean. compose() row-major world=pos+R*offset VERIFIED correct vs GetComponents convention (cframe[1..3]=pos,[4..6]=row0,[7..9]=row1,[10..12]=row2) -> rotated decks get rotated gates. deckBackGate at +Z back edge (z=maxZ, x=0, y=GATE_H/2, size={width,12,1}); evictionPoint (0,3,maxZ+4). GATE_H=12>=10 seal (distinct from 3.5 fall-guard). Minor (no-action): no deckSize-key runtime validation (mirrors SizeClasses itself). PURE MODULE LAYER (T3+T4) COMPLETE.
Task 5 (server remotes/stash/compute+push engine): complete (commits 7154062..eb362cb, review clean, Spec ✅ + Approved). 472 Lune green + rojo build OK + lint clean. 5 remotes (SetAccess/InviteUser/RevokeUser/AccessState/AccessBlocked); NetworkClient.setAccess->PUT /access; teahouseAccess stashed (default public/[]), NOT in echoEconomy; deckCFForUid/isFriend(cache)/gatesForPad/recomputeAllAccess(per-viewer AccessBlocked, invite lists stay server-side)/resolveName(filterExternalName+cache)/pushAccessState; fired on join(after echoEconomy) + economy PlayerRemoving(after nil). deckCFForUid field/arg VERIFIED vs DeckPlacement/SizeClasses + rebuildClaimedPad + deckCFForClaim (no drift). Scope/dependency order verified. Deviation: 3 selene allow(unused_variable) on not-yet-wired SetAccess/InviteUser/RevokeUser -> REMOVE in Task 6 when handlers land. MINOR for final review (PLAN-level, low-impact, do not block): pushAccessState's IsDescendantOf recheck is at top, BEFORE the resolveName yield loop -> doesn't guard the yield (plan Step 6 verbatim; FireClient-on-absent just logs). Suggested 1-line fix: move recheck to just before AccessState:FireClient. Same pattern to watch in Task 6 handlers.
Task 6 (SetAccess/InviteUser/RevokeUser handlers): complete (commits eb362cb..06cfb58, review clean, Spec ✅ + Approved, zero findings). 472 Lune + rojo build OK + lint clean. All 3 handlerQueue'd + owner-gated first + post-yield IsDescendantOf recheck AFTER net:setAccess and BEFORE mutating e.teahouseAccess; no-write-on-persist-fail (warn+resync+return); table.clone isolation (no in-place insert into live list); InviteUser guards full-list/badname(pcall+type)/self/dup each with notice+no-write; SetAccess no-op on unchanged mode; RevokeUser no-op on stale id. 3 selene suppressions from T5 REMOVED, selene clean. MAX_INVITED=50 + ACCESS_MODES locals near PREF_CAP.
Task 7 (region backstop): complete (commits 06cfb58..5693f1e, review clean, Spec ✅ + Approved). 472 Lune + rojo build OK + lint clean. task.spawn loop ACCESS_TICK=0.3; owner-gate + deckCFForUid nil-skip; viewer==owner NEVER evicted; PointToObjectSpace footprint + Y-band[-2,GATE_H]; canEnter parity w/ recomputeAllAccess (friend only in friends mode); CACHED isFriend (no per-tick web call); reads live e.teahouseAccess (handlers mutate in place); PivotTo(evictionPoint) not center. Minor (accepted): O(decks*players)/tick at ~50-player ceiling — brief-consistent tradeoff. SERVER SIDE (T5-T7) COMPLETE.
Task 8 (AccessGateController client-local gates): complete (commits 5693f1e..25b3e7c, review clean, Spec ✅ + Approved, zero findings). rojo build OK + lint clean + 472 Lune. NEW file only. folder.Parent=workspace (client-local collision technique, NOT PlayerGui); parts Anchored/CanCollide=true/CanQuery=false/CanTouch=false/Transparency=0.35/Fabric; full-rebuild-on-each-AccessBlocked (clear then repopulate -> dropped pad loses gate); layered type-guards; Vector3/CFrame from 3/12-num arrays; render-only (no teleport). AccessBlocked remote confirmed declared.
Task 9 (Access panel section): complete (commits 25b3e7c..ae2fce1, review clean, Spec ✅ + Approved). rojo build OK + lint clean + 472 Lune. 4 remote handles + access/accessNotice state; sectionLabel(82) + mode toggle(83)/invite row(84,private-only)/notice(85)/invitee scroll(86,private-only); fires SetAccess{mode}/InviteUser{username}(clears box)/RevokeUser{userId}; renderAccess highlights selected mode, private-only visibility, rebuilds invitee rows (no dup accumulation); OWNER-ONLY via vm.ownsDeck; AccessState echo -> render(). VERIFIED: per-row revoke closure captures `local uid=entry.userId` before Connect (no last-value bug); palette reused not redefined; LayoutOrder no collision. ALL CODE TASKS (T1-T9) DONE.
Task 10 (tunnel-gate survey TOOL): complete (commits ae2fce1..be76212, review clean, Spec ✅[tool-only] + Approved). stylua+selene clean (per-file, tools/ outside src/tests scope) + 472 Lune. findPadSites() datamodel-scan (NOT plan's broken ServerScriptService.Server path — Rojo maps src/server->ServerScriptService.Roshambo); place/bake modes; bake output format traced through deckBackGate->gatesForPad->AccessGateController (exact pasteable match). Implementer caught 3 real bugs in plan's snippet (dropped CFrame rotation, JS ${} interpolation=Luau parse error, unescaped braces). PAD_IDS {"T03","T14"} = editable placeholder. DEFERRED (user Studio session): Step 2 authoring real accessGates into PadSites.luau (human slab-drag; gatesForPad tolerates absent -> feature complete+functional without it; tunnel gates = optional earlier-bounce). ALL 10 TASKS DONE -> final whole-feature review next.

FINAL WHOLE-FEATURE REVIEW (07a3b14..be76212, opus): READY WITH FIXES. No Critical/Important security/correctness defect. Seams verified: AccessBlocked + AccessState producer<->consumer shapes MATCH; backstop<->recompute canEnter PARITY holds; trust boundary (backend numeric-ids-only, invite lists never leave server, names only to owner) HOLDS; owner-gate HOLDS; backstop covers walk-in/side-jump/tunnel/fall-from-above as the authoritative layer.
NEW FINDINGS (missed by per-task reviews, both gate-VISUAL desync only — backstop still enforces): (1) buy-to-claim mid-session didn't recomputeAllAccess (spec lists "pad claimed/released" as a trigger); (2) deck display-resize didn't re-push gate geometry. CARRIED Minors: pushAccessState pre-yield recheck, exact-50 untested, unknown-mode untested, no deckSize validation, O(decks*players)/tick, survey PAD_IDS placeholder + PadSites authoring deferred. Also noted: friend cache never invalidates mid-session (rare), friend/name caches not pruned on leave (negligible).
FINAL FIX (7d36e59, re-review RESOLVED): recomputeAllAccess() after terminal echoEconomy in RequestPurchase(~1036) + SetDisplay(~1112) [covers new-claim/upgrade/display-resize geometry+occupancy changes]; pushAccessState presence-recheck moved to guard the resolveName yield (top guard now e==nil/teahouseAccess==nil only; IsDescendantOf recheck immediately before AccessState:FireClient). 472 Lune + rojo build OK + lint clean.
TEAHOUSE ACCESS CONTROL CODE COMPLETE (2026-07-19, deec13c..7d36e59, 11 commits, UNPUSHED). Backend 188 Vitest + Roblox 472 Lune green + rojo build OK + stylua/selene clean.
PENDING (user): (1) DEPLOY the backend before the gate — teahouseAccess persists via PUT /players/:id/access which a stale dev backend 400s (same trap as B1/B3/B4/portal/decorations); push branch -> dev App Runner auto-deploys, OR local docker. (2) restart rojo serve (new AccessPolicy/AccessGates shared modules + AccessGateController client + TeahouseController panel changes). (3) PadSites tunnel-gate SURVEY (Task 10 Step 2, deferred): in Studio run tools/studio/surveyAccessGates.luau MODE="place" (set PAD_IDS to real tunnel-accessed pads first), drag slabs into tunnel mouths, MODE="bake", paste accessGates literals into PadSites.luau. OPTIONAL — feature works without it (deck-back gate + backstop cover all pads; tunnel gates = earlier-bounce). (4) 2-ACCOUNT VISUAL GATE (plan's post-Task-10 checklist): Public->Private bounces a 2nd player at deck-back noren + backstop on side-jump; Friends passes a friend/stops non-friend; invite-by-username (on/offline) passes; revoke evicts next tick; tunnel gate stops at tunnel (surveyed pads); owner + allowed always pass; Public decks have no gate. TUNE at gate: GATE_H/EVICT_OUT/EVICT_Y vs real deck heights; noren placeholder art (themed art pass later).


=== MAWARI-DŌRŌ LANTERN DISPLAY (Plan A) EXECUTION ===
Plan docs/superpowers/plans/2026-07-21-arena-mawari-doro-display.md. Base for Task 1 = 06f6baf (plan pre-flight commit). Pre-flight fixes applied (Lamp array color, Finial no IronDark). Plan B (chōchin+HUD retrofits) deferred, not yet written.
Task 1 (Glyphs shared module): complete (commits 06f6baf..0bb491f, review clean, Spec ✅ + Approved). 488 Lune green (4 new). Pure parts()/runtime render() split respects Lune-safety (no Color3/Instance reachable from parts). MINORS for final review (both plan artifacts, no-action): (1) chevron constants (CHEV_LEN 0.42, mids y=0.42) diverge from the derivation comment (SVG mids at y=0.5) — a deliberate upward nudge for apex-up, live gate confirms the ∧ read; (2) symbol typed `string` not `"R"|"P"|"S"` (looser than brief interface).
Task 2 (DrumStep 6→12): complete (commits 0bb491f..6e533e0, review clean, Spec ✅ + Approved, zero findings). 488 Lune green. symmetric=(i+3)%12 preserves symbol; landingStep guaranteed 1..3 steps (3 consecutive faces = full mod-3 permutation); no residual %6. ⚠️ RESOLVED by controller: DrumController is the only consumer, rewritten in Task 4 for 12 faces/90° detents.
Task 3 (ThrowDrum mawari-dōrō builder + ArenaLayout + regen): complete (impl commit 00b6af3; range 6e533e0..00b6af3 incl. a docs plan-amendment commit; review Spec ✅ + Approved). 489 Lune green + determinism passed (model.json byte-identical over 2 genmodels runs) + lint clean. Facet trig hand-verified vs generated JSON (apothem 10.625, Face0 z=-10.625). PLAN AMENDMENT (blocker): BellDrive.build sizes its yoke off throwDrum.length → kept length=11 as a documented BellDrive-only compat field (BellDrive untouched, yoke-vs-vertical-lantern mismatch = T8 defer), dropped dead yaw/southSpokeR, updated 2 stale tests (ArenaLayout.spec yaw→faces/windows; CenterpieceContract ThrowDrum lock → 12 facets/0 spokes).
  MINORS (carry to final review):
   (1) d.windows in the interface but UNREAD — posts hardcoded `for i=1,4`; harmless (value=4) but decoupled. Fix = drive the loop off d.windows.
   (2) **WIDE WINDOWS — 4 corner posts leave 90° gaps, so each window exposes ~3 facets, violating the spec's ≤30°/"one facet per window". ADDRESS AT THE TASK 4 LIVE GATE WITH THE USER (narrow the shade — solid infill panels over the non-cardinal facets). Not designed blind per stop-and-ask.**
Task 4 (DrumController — CODE): complete (commit 0065e48, review Spec ✅ + Approved). Verbatim transcription; Glyphs.render arity/types match Task 1; only Face0..11 captured as spin parts; cubic-Hermite glide + stuck-guard + gongHit/drumRest + pin-wheel applyKick preserved; local-Y spin; landTargetFor -sym*30 step π/2; SurfaceGui LightInfluence=0. ⚠️ sign convention (landing on the correct symbol) = LIVE-GATE verify (flip theta or -sym*30 if wrong). MINOR (plan artifact): heartbeat param named `_dt` but used (omega*_dt) — cosmetic, fold into any live-gate commit.
Task 4 LIVE GATE: PENDING (orchestrator-run in Studio; stop-and-ask — one attempt then show user). Carry to gate: (a) verify landing symbol (sign flip if wrong); (b) WIDE-WINDOW fix (narrow shade to ≤30°/one facet); (c) day/night look; (d) kasa cap → miniature hip roof; (e) proportions/lamp.
LIVE-GATE ART PASS (2 waves, committed ec574cb + glyphgen chore): shade→8 curve panels (one glyph/window, no parallax); 4:5 facet panels + canvas-aspect fix (round ○); miniature hip roof (Shoro wedge recipe, no rafters)+finial; diamond corner posts extended to legs; lantern lowered y148→144 onto the tower roof; base ⅓ thick; lamp dimmed (no daylight warm spill — facets opaque, warmth WAS lamp spill); DrumYoke/DrumBearing removed from BellDrive (+ dropped throwDrum.length). GLYPHS: switched native-UI → SVG-faithful IMAGE assets (glyphgen.js SDF raster → 6 white core/outline PNGs → MCP upload_image → rbxassetid in Glyphs.luau; 2-layer recolourable ink-outline+gold-core). Native UIStroke draws OUTWARD-only (lopsided ring keyline) + chevron arms wouldn't fuse → images fixed it. **PENDING: Roblox moderation of the 6 uploaded glyph assets — they render BLANK until approved (confirmed via known-asset swap test; code correct). Re-check in a few min / user Play once cleared.** 486 Lune green, determinism stable, lint clean.

---

# SDD Progress — Round Metronome (2026-07-23)

Plan: docs/superpowers/plans/2026-07-23-round-metronome.md
Base: a1c91be (plan commit). Branch m4b-zendojo-art-pass.
Task 1: complete (commit 40d279a, review clean; 189 Vitest green, +1 durations test)
Task 2: complete (commit fba6908, review clean; 531 Lune green, +4 schedule tests)
Task 3: complete (commit d0c5de7, review clean; 531 Lune green, lint clean)
Task 4: complete (commit 3183282, review clean; 538 Lune green, +7 metronome tests; math hand-verified by reviewer)
  Minor(carry to final): self:any typing vs RoundClock's typed-self convention (matches 3 other shared modules)
  Minor(carry to final): _revealSec stored but unread
  Minor(carry to final): setSchedule's now param unused; slew dt seeds from read()'s _lastNow (moot under Task 5's per-frame read)
Task 5: complete (commits fde55b3 + fix f58f26c, re-review clean; 538 Lune green; measured clock deleted, strike self-timed, striking backstop added for dropped-ACTIVE freeze [spec-gap fix])
  Minor(carry to final): double metronome:read() per frame (draw+cam heartbeats) — harmless, dedupe candidate
  Minor(carry to final): backstop leans on invariant activeSec+tallySec > period/2 (holds for real configs; unasserted)
  Minor(carry to final): pathological period ≈ revealSec+2 could still skip a strike (theoretical)
Task 6: complete (commit a77fe4f, review clean; 538 Lune green; gongStrike out of choreo, drum spin-until-data + STALL_MAX 6)
  Minor(carry to final): RoundUpdate stuck-guard 4s threshold predates STALL_MAX (legit worst case now 9s) — inert scaffolding, out of scope here
Final whole-branch review (fable): READY after one fix — drum stuck-guard 4s->10s + consume throw (95fa7cc, applied+verified). Deferred: phaseEndsAt 1s-tick quantization => schedule republish never converges, slew absorbs (follow-up: exact phase-start ms in RoundEngine or widen gate to ~1.2s); minors a-f deferred (see triage in review).
Task 7: local verification GREEN (538 Lune + 189 Vitest + builds + lint, no asset drift). Pushed a1c91be..95fa7cc. Awaiting LIVE PLAY GATE (user).
Task 7 LIVE PLAY GATE: PASSED (user: "pretty dang good"). Gate-tuning wave after the plan (all committed+pushed, CI green):
  c845fb7 clearance 0.5->0.1; 988b920 dowel-radius shave (superseded); 0046283 EXACT-LOCUS flank + CamPhaseDeg->0; 16b4b01 clearance 0.03 visual touch; 8ab3ba7 lockstep (server exact phase clock + client shared-timeline cam) — deploy verified live (phaseEndsAt stable).
== ROUND METRONOME COMPLETE: a1c91be..8ab3ba7, gate passed 2026-07-23. Deferred minors a-f in triage above. ==

---

# SDD Progress — Karesansui Square (2026-07-24)

Plan: docs/superpowers/plans/2026-07-24-karesansui-square.md
Base: 90fc10a (plan commit). Branch m4b-zendojo-art-pass. HYBRID: T1/T6/T7 main-session (Studio+gates), T2-T5 subagents.
Task 1: complete (commit 06ce117, main-session; survey: Shoro eave→x16/z18, Overlook face x54, spawn 26..34/4..12; panels east{18,-6,34,2} south{-12,20,10,28}, corridors east{34,-10,54,38} shop{-20,28,34,44}; 543 Lune green +4)
Task 2: complete (commit d04f4dc, review clean; 549 Lune green +6; asset 18 parts, corridors inviolate, no drift)
Task 3: complete (commits 7acac3c + fix d8b1641; four PNGs verified visually — pitch N/10=51.2px [plan's 64px comment was wrong], tangent-space normals fixed blue-dominant, albedos byte-identical across fix)
Task 4: complete (commit bb83811, review clean; 552 Lune green +3; winding cross-product-verified by reviewer)
  Minor(carry to final): degenerate geom (amplitude=ridgeW=0) → NaN normals, unguarded (no call site passes it)
  Minor(carry to final): triangle winding has no CI regression net (analytic normals pass either way) — gate 3 visual covers it
Task 5: complete (commits 22c319b + fix dcb59af, review approved; idempotency guard added — re-tag refreshes rings)
  Minor(carry to final): no bounds check vs garden panels — a mis-tagged plaza rock gets rings at fieldTopY (place-curation responsibility)
Tasks 6+7 (main-session Studio gates): COMPLETE. Gate history: fields buried (per-panel floorY fix) -> postage stamps -> L -> ONE garden {-21,-12,34,18} containing the machine (user-driven, +iterative edge tuning); kerb 1.0 proud/2.5 root; shoro plinths +0.5; spawn moved to east corridor x40. FIELD MATERIAL: ambientCG Gravel041 desat 0.30 + symmetric-sinusoid furrows (groovegravel.py; profile journey cosine->triangle->scallop->sinusoid — the EYE reads SHADING, not height), SPT 5.2. STONES: 3 (sanzon; smallest stood vertically ON THE BELL AXIS at (-2,0) — user's idea, the triad centre), Rock058 via ZenGardenStone variant SPT 7. RINGS v2: solid faceted disc (v1 separate ridges = moire vs field furrows), soft 0.5, pitch 0.35, RingFootprintR attr override (Medium 2.2), RakedGravelRings variant (no normal map). GATES 2/3/4 PASSED (screenshot 8.13pm "there it is"). Bonus: strike flash + energy rings CUT (bell sound tomorrow).
== KARESANSUI SQUARE COMPLETE a1c91be-era plan 90fc10a..849bf79; 554 Lune green; CI green. Place save PENDING (variants, stones, tags, CamPhaseDeg=0). ==
Place SAVED (user-confirmed 2026-07-24 night). Splash also cut (c2beffc) — strike fully bare for the bell sound.
