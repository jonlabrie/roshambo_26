# SDD ledger — plan: docs/superpowers/plans/2026-09-05-fireworks-shows-sequencer.md

Spec: docs/superpowers/specs/2026-09-05-fireworks-show-system-design.md (approved 2026-09-05; §1–§3, §10 row B, §12).
Worktree: .worktrees/shows, branch thread/shows, base 5806241 (main). Baselines: server 584/584; Luau 1846/1846; stylua+selene clean.
Models: implementers opus (owner's standing choice); task reviewers opus; small re-reviews sonnet; final review fable.
Two-session repo: the terminal session owns main for code; this branch merges through it (finishing skill presents options; no self-merge).

## Pre-flight scan

| tasks | produces → consumes | finding |
|---|---|---|
| 1→2 | fixture cases, error codes, zero-based `cue` | identical code lists; both twins index cues from 0; TOO_LONG case at 300001 ms vs limit 300 s works on both |
| 1→4 | `validateShow/tallyShells/DECK_STAGE/Cue` | route uses exactly these; `shellMortar` may be unused in the route — plan says drop it from the import if so |
| 2→5,6 | `ShowPlan.validate(cues, stage, shellMortar, knownShells)`, `DECK_SLOTS`, `PROVING_SLOTS`, `knownShellSet` | T5 builds knownShells from `FireworkCatalog.RECIPES` keys (no `SHELL_IDS` on the Luau side — verified); PROVING_SLOTS keys = `ProvingPlan.RACKS` (6 names incl. "hanabiya roof") |
| 3→5 | `ShowPlayer.schedule(busyUntilMs?, nowMs, cues)`, `timeline`, `delaysFrom` | used with matching argument order |
| 4→5 | reserve response `{reservationId,...}` via `NetworkClient.Result.data`; body `{ show }` | T5 reads `res.data.reservationId`, logs `res.status/res.error` — Result fields verified |
| 5→6 | `broadcastLaunch`, `playShow`, `rollBoost`, remote `RequestProvingShow` declared in T5 | consistent; T6 adds `provingOriginFor` after `stageForProving` |
| 6 | finale_v1 arithmetic | 15+6+20+6+5+8+20+6+5+6+12 = 109 cues ≤ 120; volley(…,250,true) puts 6 cues within 200 ms → the ≥6-in-300ms test holds |

Per-task self-consistency: T1 test imports the JSON fixture (as fireworks.test.ts does); T1 module holds literals (RULING below). T2/T3 tests exercise exactly the exported names. T4 concurrency test mirrors the existing spend concurrency test. T5/T6 have no Lune coverage (main.server.luau cannot load under Lune) — verification is the Studio check + stylua/selene; reviewers will ⚠ this and the controller resolves it against the plan's stated posture. T7 is docs + push + STOP.

Ruling (pre-flight): `server/src/shows.ts` holds `SHOW_LIMITS`/`DECK_STAGE` as literals and `shows.test.ts` asserts them equal to the fixture — the plan's original `import fixture` in runtime code would fail `tsc` (`rootDir: ./src`) and the deployed container is built from `server/` alone. Same pattern as GameRules.ts vs game-rules.json. Cost if wrong: none; the test pins equality.

Rubric-sensitive mandates: T5 extracts `rollBoost`/`broadcastLaunch` from a 1700-line file with no harness — a behaviour-preserving refactor verified by reading + the Studio check.

## Tasks
Task 1: implementer a266b94600ff628a3 (opus); DONE_WITH_CONCERNS at 674add4 (concerns are observations: error precedence unpinned by the fixture — order BAD_CUE→NEGATIVE_TIME→CUES_OUT_OF_ORDER→BAD_SLOT→BAD_SHELL→TIER_MISMATCH, TOO_MANY_CUES first, TOO_LONG last; unknown stage-slot values degrade to "any"; TOO_LONG is strict >). Carried into Task 2's dispatch so the twin matches.
Task 1: reviewer aae6fe7cc49591685 (opus): Needs fixes — 1 Important: `stage[c.slot]` reads Object.prototype (slot "toString"/"constructor"/"__proto__") so validateShow THROWS instead of returning BAD_SLOT; invisible to the fixture and cannot occur in Luau. Fix round 1 dispatched (fresh implementer; resume unavailable).
Task 1: minor (deferred): TOO_LONG's absent `cue` not pinned by the fixture loop (toEqual would pin it); Number.isFinite/NaN/Infinity/non-integer t_ms unreachable from JSON — carry to Task 2 so the twin matches (`type(t)=="number"` alone would accept math.huge); non-array cues → EMPTY lives only in the TS test (a `cues: null` fixture case would pin the twin); per-cue error precedence unpinned (two multi-fault fixture cases would close it — Task 2 candidate)
Task 1: fix round 1/5 (1 addressed, 0 open — own-property slot lookup + accepts-shape check → BAD_SLOT; fixture case "toString"; commits 674add4..5de8444); fix implementer a81e3b2f089029c79 (opus); re-reviewer ac21962ece1862949 (sonnet)
Task 1: complete (commits 079b1b9..5de8444, review clean; trailers verified)
Task 2: implementer a7801256887b4c2a5 (opus); DONE at f1488c8; Luau 1871/1871; stylua+selene clean; mutation-checked the three hand-written rule tests. Concerns: stylua reported non-idempotent on the new spec (two passes to a fixed point); neither twin guards a non-table `stage` (throws) — stages are server data, noted as deferred minor.
Task 2: reviewer a5e0aea1128f255ef (opus): Approved.
Task 2: minor (deferred): `ipairs` stops at a hole while TS walks by index — unreachable from JSON, reachable from a hostile RemoteEvent payload → CARRY TO TASK 5: RequestShowGo must reject a cues table whose `#` disagrees with a pairs count (or densify first) before calling validate; no test for fractional t_ms accepted; nil `stage` throws in both twins (stages are server data); mixed iteration idiom; shows.ts has no PROVING_SLOTS counterpart (fixture's proving stage asserted on the Luau side only)
Task 2: complete (commits 5de8444..f1488c8, review clean; trailer verified)
Task 3: implementer a748902eb426c6646 (opus); DONE at 8beae7a; Luau 1879/1879; stylua+selene clean.
Task 3: reviewer abaee60b81c0b9823 (sonnet): Approved.
Task 3: minor (deferred): `Cue` type redeclared locally rather than imported from ShowPlan (structural twin; drift risk only)
Task 3: complete (commits f1488c8..8beae7a, review clean; trailer verified)
Task 4: implementer aae86f383862ee962 (opus); DONE at 5823fe5; server 612/612 + tsc clean; Luau 1880/1880 + stylua/selene clean. Concerns noted: reservationId is a random token, not persisted (fine for B — used as a log/credit label; sub-project D's bookings persist); no refund path (spec §2.1: once lit, lit — by design); `held` in INSUFFICIENT is a pre-update read (diagnostic only).
Task 4: reviewer afad7cf7786300b7a (opus): Approved.
Task 4: minor (deferred): reservationId is Math.random base36 (brief-specified; theoretically <6 chars; not collision-proof — use crypto.randomUUID and persist when reservations become load-bearing, sub-project D); the no-`cue` 400 branch untested (an `EMPTY` toEqual case would pin it); the 200 echoes the caller's raw cue objects (project to {t_ms,slot,shellId} before ever persisting)
Task 4: complete (commits 8beae7a..5823fe5, review clean; trailer verified)
Task 5: implementer ab3ef947f25cc1d11 (opus); DONE at c9beca5; stylua/selene clean; Luau 1880/1880; Studio check NOT run (no Studio) — carried to Task 7's gate. Densify guard (Task 2 carry) added before validate.
Task 5: Ruling: the brief's ten new top-level locals in main.server.luau do not compile — the file is at Luau's 200-local register ceiling (tests/Compiles.spec.luau caught it: "Out of local registers"). The implementer hung everything off one `local Launch = {}` namespace (rollBoost, broadcastLaunch, playShow, denseCues, the handler) + a `type ShowCue` alias; net cost 1 register, 1 slot left. ACCEPTED — forced by the file, behaviour identical; the plan's names become `Launch.*`. CARRY TO TASK 6: `RequestProvingShow`, `provingOriginFor` and the `FireworkShows` require must hang off `Launch`, never new top-level locals. Cost if wrong: none functional; readability of a file that already needs splitting. FOLLOW-UP (backlog, not this plan): main.server.luau needs real module extraction — the register ceiling now binds every future server change.
Task 5: reviewer a0e9d390f3ffb2e90 (opus): Approved; independently re-measured the register ceiling (exactly one top-level slot left).
Task 5: Ruling: the reviewer's observation is a REAL spec gap (spec §2.4 "owner leaves mid-show: playback continues") — PlayerRemoving nils playerEconomy[uid] and muzzleOriginFor then returns nil, so every remaining MORTAR cue is skipped while the reserve already debited the whole show. Fix round 1: snapshot each used mortar slot's origin/heading at go (one muzzleOriginFor per distinct slot, before playShow) and serve cues from the snapshot; hand cues already use the captured frame. Cost if wrong: none; origins do not change during a show. The mangled handler comment (:1761) is folded into the same round (same file, trivial).
Task 5: minor (deferred): `delays[1]` unguarded in playShow (safe today: validate rejects EMPTY; Task 6 reuses playShow for drafts — the drafts spec asserts non-empty); mid-script `WaitForChild("RequestShowGo")` without a timeout would stall the rest of the script on a stale place (add a timeout + nil guard when Task 6 adds its remote)
Task 5: fix round 1/5 (2 addressed, 0 open — mortar origins snapshotted at go per distinct slot; handler comment restored; commits c9beca5..92c3639); fix implementer aaf0a1d46c39609ce (opus); re-reviewer a53a6f889c7350c02 (sonnet); Compiles.spec green (1 register slot left)
Task 5: complete (commits 5823fe5..92c3639, review clean; trailers verified)
Task 6: implementer aefcfd8ba8dfe24cb (opus); DONE at ea71393; Luau 1887/1887; stylua/selene clean; finale_v1 = 109 cues, last at 71.3 s, densest 6 in 300 ms; register slot still unspent (measured). Concerns: provingOriginFor checks "hanabiya roof" before the rack lookup (old handler reversed; inert — the roof is virtual, no rack part of that name); proving shows share one pity-ramp streak under uid "proving"; Studio unverified.
Task 6: reviewer a93285d3cbad531c8 (opus): Approved; proved the "hanabiya roof" reorder unreachable (ProvingGround is Rojo-managed with exactly five station parts).
Task 6: minor (deferred): the `FireworkShows` require is an untimed WaitForChild immediately above the timed remote fetch (mitigation buys nothing — time both or neither); Play button abuts the scrollbar (x 334 of 340) — eye it in Studio; `boostMisses["proving"]` never cleaned (session-lived, bounded)
Task 6: complete (commits 92c3639..ea71393, review clean; trailer verified)
Task 7: implementer aa54d2ac2a96e133a (opus); DONE at f2f0b2f; wiki lint 36/7 before and after (branch baseline, pre-existing); pushed thread/shows; roblox-ci success https://github.com/jonlabrie/roshambo_26/actions/runs/33991784924 ; server-ci success https://github.com/jonlabrie/roshambo_26/actions/runs/33991784970 ; no merge. Section placed before "## Raw layer" (raw layer stays last — fine).
Task 7: reviewer ad2ee8c30bd82cc6e (sonnet): Needs fixes — 1 Important: the log `ship` entry does not say the work is unmerged on thread/shows (every comparable in-flight entry does). Fix round 1 dispatched.
Task 7: fix round 1/5 (1 addressed pending re-review — "NOT merged" sentence appended; commits f2f0b2f..c1dfebe); fix implementer afa5f84be8941cd7d (sonnet). Re-review and the final whole-branch review dispatched in parallel (the fix is one sentence; if the re-review reopens it, the final package is regenerated).
Task 7: re-reviewer af3155239b4f6ebd4 (sonnet): addressed; lint 36/7 unchanged.
Task 7: complete (commits ea71393..c1dfebe, review clean; trailers verified). All seven tasks complete.
Final review: first dispatch (agent ac8df52f851bf5380, fable) terminated by the session rate limit mid-read; re-dispatched.
Final review: agent aef4c63cb418ff72d (fable). Verdict: With fixes. Important #1: muzzles snapshotted AFTER the reserve — a leave during the HTTP round-trip (or an unresolvable slot: bare pad, e.mortars lag) still debits then skips; fix = snapshot + refuse-if-unresolved BEFORE reserve. #2: denseCues's `#t == count` is not a density guarantee (`{1,3,5}` with array border 3 passes) — require maxKey == count, project to {t_ms,slot,shellId}, move to ShowPlan.dense() with a Lune test, correct the wiki sentence. #3: spec §1/fixture/wiki claim condition shells (ishibana) are "re-checked per cue at fire time" — nothing does; the hand path has the same client-only gate. #4: the three mid-script WaitForChilds are inconsistent (RequestShowGo untimed; FireworkShows require untimed; RequestProvingShow timed). Minors: rollBoost recreates boostMisses for a departed owner; show.title unbounded; raw cue echo; TS twin lacks the t_ms==300000 and NaN/±inf tests; reservationId regex flake; OwnLaunch per show cue may hunt the owner's camera (eye it on the A13 run); stageBusyUntilMs never pruned.
Final: Ruling on #3 — take option (b), DOCS ONLY: the condition on `ishibana` is a client-side launchable gate on EVERY path today (the hand spend route never checks it either); amend the fixture comment, spec §1 and the wiki to say so, and put "per-cue condition check at fire time" on sub-project C's list, where the composer lands and where `lastWorldThrow()` is one call away. Why: implementing it now means a Luau CONDITIONS mirror + fixture gating for a rule the shipped hand path also lacks; the claim is the bug, not the behaviour. Cost if wrong: on dev only, a reserved ishibana can fire without Rock — no production client can send RequestShowGo until C.
Final: Ruling on rulings-check — the reviewer is right that my Task 5 ruling 2 ("cost if wrong: none") was overconfident: the snapshot had to precede the debit. Recorded.
Final: fix wave dispatched (one implementer): #1, #2, #3(b), #4, plus minors: title cap, cue projection, TS twin edge tests, wiki camera-hunt note. Deferred: rollBoost departed-owner table, reservationId, OwnLaunch gating, stageBusyUntilMs pruning.
Final: fix wave implementer ab26746dd649e6e05 (opus) → daded6d (code) + 6676bdb (docs). Luau 1892/1892 (5 new dense tests), server 614/614 (TS edge tests RED by mutation), stylua/selene clean, lint 36/7 unchanged, Compiles.spec green (Launch.denseCues freed; no top-level local spent). Pushed.
Final: re-reviewer aaf08a93c8b3fb587 (opus): all findings ADDRESSED, no new breakage; Luau 1892/1892; lint 36/7. Out-of-scope: `#show.title <= 80` is a byte count (fails closed; utf8.len if titles are ever Japanese).
Final: whole-branch review clean after the fix wave. Branch thread/shows at 6676bdb ready for finishing-a-development-branch. Owner items: the Studio check (Task 5 Step 6) and the A13 gate (finale_v1) before merge; merge goes through the main thread.
