version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm install --prefix server
      - npm run build --prefix server
run:
  command: node server/dist/index.js
  network:
    port: 3001
  env:
    - name: PORT
      value: "3001"
    - name: MONGODB_URI
      value: "mongodb+srv://jonlabrie_db_user:eYBOQwdAkGgU2jnC@roshambocluster0.ckjseml.mongodb.net/?appName=RoshamboCluster0"
    - name: JWT_SECRET
      value: "roshambo_super_secret_1337"

