-- ZenDojo TEAHOUSE PERCHES — scouted site list (2026-06-20), the bake input for the
-- future parameterized Teahouse builder (canyon village sub-project 2). Captured live
-- from the player's position+facing while scouting; stamped (rough) into
-- workspace.CanyonTeahouses via clone-of-TeahousePrototype + teahouseUpgrade recipe.
--
-- Each: name, pos = deck-ish world point at the ledge, face = veranda look direction (XZ).
-- These are ROUGH placements to give the paths their targets; positions will be nudged
-- and ultimately regenerated by the Teahouse builder from this list (-> ArenaLayout.teahouses).
-- More perches pending the WESTERN-WALL/HEAD + FLOOR terrain work (waterfalls + step pools);
-- the lowest wall perches (11, 12) may need a recipe re-run after that terrain changes.
-- hand = engawa wrap side ("right" = base TeahousePrototype, "left" via makeLeftHand / ServerStorage.TeahousePrototypeL).
-- FINAL HANDS (2026-06-20): left = {03, 05, 06}; all others right.
-- NOTE: all 12 were hand-tuned in-scene (rotated/moved/raised, pads cut on 05-12) after stamping, so the
-- pos/face below are the original SCOUTING SEEDS, not final — the placed models in workspace.CanyonTeahouses
-- are the source of truth. Re-derive exact transforms from those instances when porting to the builder.
return {
	{ name = "Teahouse_01", pos = { 43.2, 204.5, 128.3 }, face = { -0.34, -0.94 }, wall = "near", hand = "right", note = "lower; stacked above the TeahousePrototype cleft (NearWall_12)" },
	{ name = "Teahouse_02", pos = { -141.4, 173.7, 74.2 }, face = { -0.54, -0.84 }, wall = "near", hand = "right", note = "by NearWall_30; pulled fwd+up in-scene" },
	{ name = "Teahouse_03", pos = { -188.4, 190.5, 73.0 }, face = { -0.50, -0.87 }, wall = "near", hand = "left", note = "by NearWall_40; LEFT-hand; moved fwd+up, rotated in-scene" },
	{ name = "Teahouse_04", pos = { -192.3, 221.4, 103.1 }, face = { 0.97, -0.25 }, wall = "near", hand = "right", note = "high; faces down-canyon; rotated in-scene" },
	{ name = "Teahouse_05", pos = { -322.1, 219.2, 85.6 }, face = { 0.63, -0.78 }, wall = "near", hand = "left", note = "by NearWall_62; upper; LEFT-hand; rotated+raised+pad" },
	{ name = "Teahouse_06", pos = { -381.7, 222.6, 65.0 }, face = { -0.80, 0.60 }, wall = "near", hand = "left", note = "head; faces hero falls; LEFT-hand; rotated 180+raised+pad" },
	{ name = "Teahouse_07", pos = { -322.2, 196.8, 44.6 }, face = { -0.93, -0.37 }, wall = "near", hand = "right", note = "lower tier below 05; rotated+pad" },
	{ name = "Teahouse_08", pos = { -360.8, 242.8, -106.6 }, face = { 0.98, 0.21 }, wall = "far", hand = "right", note = "moved E to FarWall_72, lowered, rotated+pad" },
	{ name = "Teahouse_09", pos = { -273.4, 212.4, -63.6 }, face = { -0.50, 0.86 }, wall = "far", hand = "right", note = "by FarWall_70; rotated, pulled SW+pad" },
	{ name = "Teahouse_10", pos = { -191.0, 213.5, -98.8 }, face = { 0.63, -0.78 }, wall = "far", hand = "right", note = "by FarWall_62; rotated, pad, pulled SE (tucked under cliff)" },
	{ name = "Teahouse_11", pos = { -187.9, 185.0, -65.8 }, face = { -0.10, 1.00 }, wall = "far", hand = "right", note = "lower tier below 10; near floor zone; rotated+pad" },
	{ name = "Teahouse_12", pos = { -122.8, 166.4, -67.5 }, face = { 0.72, 0.69 }, wall = "far", hand = "right", note = "descending down-canyon; near floor zone; rotated+pad" },
	{ name = "Teahouse_13", pos = { 3.0, 202.9, -144.5 }, face = { 0.11, 0.99 }, wall = "far", hand = "right", note = "down-far-wall stretch (z-144); faces across the gorge; pad cut" },
}
